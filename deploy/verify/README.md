# Disposable Toolkit verification stack — Memory, Replicator and Diagnostics

Use ONLY `deploy/verify/compose.yaml` on a fresh disposable OpenHands sandbox-local Docker daemon with a fresh project. Never combine with production `deploy/docker-compose.yml`, connect to operator Docker, use `osjs-data`, or run `down -v`. Retained earlier volumes `mcsverify-1789784184-232_verify-data` and `mcsverify-rep-005-20260919_verify-data` must NOT be reused or removed. If sandbox dockerd is idle, try safe `sudo dockerd` startup as permitted tooling prep before calling BLOCKED. Verify context/daemon identity, resource labels, production resource absence and loopback UI port each run.

## Isolation and topology

Five test-only services: one-shot `seed`, `mma2`, `modbus-simulator-runtime`, `modbus-replicator-runtime`, `osjs-shell`. Each NEW project gets a NEW `verify-data` volume: seed refuses ANY nonempty volume and writes disposable `listeners: []`, Simulator `devices: []`, Replicator `devices: []`. Never weaken/bypass/re-run seed on populated volume. Both Go runtimes share only test MMA2's private internal network namespace; test source `127.0.0.1:15020`, destination `15021` and intentionally unreachable source `127.0.0.1:15999` are entirely sandbox-private. No customer/host/LAN/Internet source. OS.js exposes ONLY `127.0.0.1:18219` for browser access over separate verify-ui network; no host-published Modbus port. All services share only the new project volume for Unix sockets/config. Test MMA2 runs root inside unprivileged container for restart acknowledgement. No fixed container names, host network, external or production data mounts.

## One-shot seed / restarts

The archived UMIG-004-V `docker compose start modbus-simulator-runtime` command FAILED when Compose re-evaluated seed against populated volume. Do not repeat `compose up/start` after initial creation. If a future exact packet explicitly needs a single-service restart, first verify ONE exact stopped container ID and its project/service labels, then `sudo docker start <verified-ID>`; no guessing or bypass. UMIG-005-V and UMIG-006-V instead induce synthetic source loss/recovery through bounded GUI changes and require NO direct container stop/start.

## Authority and cleanup

ONLY the CURRENT sole ACTIVE JR packet in `handoff.md` authorizes exact actions. The UMIG-005-V live report is archived; its commands are historical, not authority to repeat. For UMIG-006-V, Diagnostics is read-only, and the carried-forward full Toolkit WINDOW close/Start-menu relaunch must be directly observed while a synthetic canonical Replicator device exists. Configure and later delete test devices via UI only; compare config hashes/owners and note genuine errors. On product contradiction, STOP product testing, retain factual evidence and perform only safe owned-resource cleanup. Before teardown verify all container, network and volume `com.docker.compose.project` labels, then exact-project `docker compose ... down --remove-orphans` WITHOUT `-v`; retain exact test volume with its label, never remove a volume or run prune/wildcard cleanup. No production commands, ICC changes, Windows COMMS, launcher cutover or acceptance of unavailable unsupported capabilities.
