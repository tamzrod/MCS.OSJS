# Handoff

## Direction and task authority — 2026-09-19

Human approved staged MCS.OSJS Toolkit migration, UMIG-005 Replicator CODE and independently authorized test stages. ChatGPT owns CODE, narrowly authored task-specific JR packets, evidence adjudication and workflow advancement. OpenHands/JR runs ONLY exact current TEST/VERIFY packets under general `operation cwal.md`, edits ONLY the authorized JR report section, pushes ONLY `handoff.md`, then STOPS. Do not modify the general CWAL directive to correct task-specific authoring gaps. Only BLACK SHEEP WALL edits ICC. Preserve production Docker/`osjs-data`/operator settings, legacy OS.js packages and Windows Electron. Never touch production daemon, remove any volume or use `down -v`.

## SOLE ACTIVE — UMIG-006 CODE (ChatGPT), NOT TEST

UMIG-005-T independently passed UNIT/BUILD (report commit `4bbe4df852c5ab38c8081abdc15047279b8ceff4`), archived in previous advancement `d3e3d9e3d087c6d20916a22b73c5ae02c2a36606`. UMIG-005-V JR live behavior report is preserved in full at immutable `handoff.md` commit `1a04664e5fdc21e3bd323a97a4f12f3f9d39abdd`; GitHub compare `d3e3d9e..1a04664` verifies it changed ONLY `handoff.md`. Reviewer accepted observed core live behavior with EXPLICIT LIMITATION: JR observed canonical YAML persistence, tab switching/re-selection, real FC3 polling, ownership conflict protection, truthful ERROR/recovery, test-only deletion/cleanup, but the exact step-8 real Toolkit window close/reopen/reload was NOT demonstrated. Do not describe that specific check as PASS. Archive `workflow/archive/umig-005-v-replicator-runtime.md` records the disposition and retained test volume `mcsverify-rep-005-20260919_verify-data` (never reuse/delete without distinct permission). Next already scheduled rendered stage `UMIG-006-V` owns this missing explicit GUI check; until it passes, no final migration/cutover acceptance from the claimed reopen observation.

**Only ACTIVE task: `workflow/active_work/umig-006-adapt-diagnostics-tab.md` (CODE / ChatGPT).** `UMIG-006-T` and `UMIG-006-V` remain QUEUED; UMIG-007+ QUEUED, UMIG-008/009 cutover/removal Planning only, DOCKER-001 paused. Do not treat this handoff as authorization for JR to run anything now.

## UMIG-006 CODE scope and checkpoint

Adapt only Toolkit Diagnostics to actual supported OS.js Memory/Replicator/MMA2 status/error observations. Preserve the frozen donor visual structure and ShadowRoot CSS isolation; never turn missing data green or present Windows service, executable path, or controls as operational. Unsupported Start/Stop or other privileged actions remain disabled/inert. Reuse existing contracts/transport and prepared dormant `diagnostics-model.js` where its exact shape fits; never import legacy UI, add new HTTP endpoint, independently restart backend, modify production Compose, touch operator data or rewrite Memory/Replicator implementation. Author focused deterministic tests for JR but do not run or claim JR tests. Record source/diff/readback and unsupported observations; only after CODE source checkpoint is reviewed should ChatGPT archive UMIG-006, promote sole ACTIVE UMIG-006-T and write its own exact bounded JR packet with explicit test commands, expected evidence, stop rules and report authority. No JR test instruction is currently active.

## Next action and recommendation

Next action (ChatGPT): implement the sole ACTIVE UMIG-006 Diagnostics CODE checkpoint. Recommendation: use actual supported statuses and defer the one missing Replicator full-window reopen observation to the already planned UMIG-006-V browser test; do not rerun the entire UMIG-005-V stack or edit `operation cwal.md` merely to correct packet wording.
