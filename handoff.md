# Handoff

## Human direction, role and immutable evidence — 2026-09-19

Human approved staged MCS.OSJS Toolkit integration AND creation of an isolated disposable verification environment. ChatGPT owns CODE/environment source changes, JR evidence review and workflow advancement. OpenHands/JR follows `operation cwal.md`, TEST/VERIFY only, modifies ONLY the authorized JR report below and pushes ONLY `handoff.md`. Only BLACK SHEEP WALL edits ICC. Preserve the operator's working Compose deployment, all user/production configurations and `osjs-data`, legacy apps and separate Windows Electron. Never run production Docker commands or `docker compose down -v`.

UMIG-003-T static/build PASS at `b002c9f852456b3447ae0bef41ae4f69eb3073e1`; UMIG-003-V fixture-browser PASS at `6f82196700b1c312652fdd7584a508e5452c4822`; UMIG-004 CODE source-only `508c6b031e675c696de3ff7bdb4291dd005d54ae`; UMIG-004-T independent unit/build PASS at `0eba36e61ec30254ddccb19bb8ff88ff403131cf`. Corrected UMIG-004-V TARGET PREFLIGHT READY JR report is preserved at immutable `37dc61f533c8e7221610c8bf5858ddbe01790312` (`handoff.md`), replacing the previously incorrect `e5a1be9` BLOCKED interpretation. That corrected JR commit modified ONLY its authorized handoff report. The OpenHands ephemeral sandbox successfully started its own freshly initialized Docker daemon (`85b789e1-f930-4fc0-968c-935af6ba63f5`), `hello-world` exited 0, containers and volumes were empty, production names/volume absent and port 18219 free. This is eligible disposable sandbox evidence, NOT live Toolkit VERIFY or proof of production health. Do not demand a separate VM merely because dockerd was idle.

## Current task and target

**SOLE ACTIVE: UMIG-004-V — LIVE Memory VERIFY, independently NOT RUN/NOT PASS.** UMIG-004-T archived COMPLETE; UMIG-005 QUEUED (Previous UMIG-004-V); later UMIG tasks gated. DOCKER-001 queued/paused; UMIG-008/009 cutover/removal remain Planning and separately human-gated.

Accepted test target: existing disposable OpenHands runtime sandbox `runtime-ylrnihathakoylzg-68df6b6fb9-6cgp6` with newly initialized sandbox-local daemon ID `85b789e1-f930-4fc0-968c-935af6ba63f5`, DockerRootDir `/var/lib/docker`, Unix socket `/var/run/docker.sock`, zero initial containers/volumes, production names and `osjs-data` absent. A recycled OpenHands sandbox is permitted ONLY if it independently satisfies all the same fresh/empty/ownership invariants; record its new identity. No production daemon or a generic second project on it is authorized. Use fresh project `mcsverify-1789784184-232` from the successful preflight IF its project resources remain absent; otherwise STOP/BLOCKED, do not improvise an existing-project cleanup. Test UI loopback port 18219; synthetic MMA2 port 15020/unit 1 INSIDE the test runtime namespace ONLY. `deploy/verify/compose.yaml` and `deploy/verify/README.md` are the only test stack definition and guide. A bounded source-only amendment after preflight seeds a genuine empty canonical `devices: []` document and runs MMA2 test container as root for `/data/config/mma2/restart-ack` permissions; the updated Compose needs fresh read-only inspection. No product or production deployment source changed. Missing-device-file Go JSON null behavior is OUTSIDE this test and remains unverified.

## JR TEST TASK — CURRENT: UMIG-004-V LIVE VERIFY

GOAL: In the accepted OpenHands disposable sandbox only, perform direct real-browser Toolkit Memory load, explicit Save & Apply against test Simulator/MMA2, verify canonical persistence and selected-device truthful status/None-Random, verify runtime outage and recovery, clean up only owned test containers/networks, and return raw evidence. This is independent LIVE VERIFY; previous unit/build or preflight evidence CANNOT substitute for any required observation.

SAFE SETUP / MANDATORY STOP GATES (run in order):
1. In the fresh disposable checkout of `tamzrod/MCS.OSJS` at `origin/main`, record `git rev-parse HEAD`, `git status --porcelain` (must be tracked-clean) and `git merge-base --is-ancestor 37dc61f533c8e7221610c8bf5858ddbe01790312 HEAD` (exit 0). Confirm `workflow/active_work/umig-004-v-memory-runtime.md` SOLE ACTIVE, UMIG-004-T archived COMPLETE, UMIG-005 QUEUED. Read this packet and `deploy/verify/README.md` and `deploy/verify/compose.yaml`. If shallow clone prevents ancestor check, `git fetch --unshallow origin` is allowed read-only preparation; no reset/clean or tracked changes.
2. Use ONLY the disposable OpenHands sandbox's own daemon. Record `hostname`, `id`, `docker context show`, `echo "${DOCKER_HOST:-unset}"`, `sudo docker info --format '{{.ID}} {{.Name}} {{.DockerRootDir}}'`, `sudo docker ps -a --format '{{.Names}}'`, `sudo docker volume ls --format '{{.Name}}'`, `sudo docker network ls --format '{{.Name}}'`, `sudo docker compose version`, and `ss -ltn`. If its existing sandbox-local daemon is idle, starting `sudo dockerd` locally and checking the log/socket is permitted environment setup; do not start or connect to a remote/operator daemon. Confirm identified OpenHands sandbox, local Unix endpoint, freshly isolated daemon (original ID above OR a newly initialized replacement in a fresh OpenHands sandbox), ZERO unrelated containers/volumes, all production container names and `osjs-data` absent, free UI port 18219. If provenance, context, resource ownership or port is ambiguous, BLOCKED and STOP BEFORE startup; no new VM requirement.
3. From repository ROOT set `PROJECT=mcsverify-1789784184-232` and `MCS_VERIFY_OSJS_PORT=18219` in this session; record values. Inspect `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml config` (exit 0): exactly seed/mma2/modbus-simulator-runtime/osjs-shell; empty-volume refusal; seed canonical `devices: []`; mma2 `user: 0:0` test-only; internal MMA2 network and Simulator `network_mode: service:mma2`; separate UI bridge bound ONLY 127.0.0.1:18219; NO container_name, privileged, host network, external/bind volumes or host Modbus ports. Confirm `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml ps -a`, `sudo docker ps -a --filter "label=com.docker.compose.project=$PROJECT"`, `sudo docker volume ls --filter "label=com.docker.compose.project=$PROJECT"`, `sudo docker network ls --filter "label=com.docker.compose.project=$PROJECT"` all show zero pre-existing project resources. If any mismatch, BLOCKED/STOP without build. DO NOT combine with `deploy/docker-compose.yml`.

EXACT TEST COMMANDS / GUI ACTIONS (on the same accepted sandbox, in order; STOP further product testing on first contradiction, but perform ONLY verified owned-resource cleanup):
4. From repo ROOT: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml up -d --build` (record stdout/stderr/exit and image identities; no other Compose file). Inspect `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml ps -a` and `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml logs --no-color --tail=120 seed mma2 modbus-simulator-runtime osjs-shell`. Expect seed exit 0, three long-running test services, no startup errors. Wait at most 180 seconds for `curl -fsS http://127.0.0.1:18219/healthz` to return HTTP 200 `{"status":"ok","shell":"neutral"}` and for `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml exec -T modbus-simulator-runtime sh -c 'test -S /data/run/modbus-simulator.sock'` to exit 0. If a required image/tool cannot safely be obtained after reasonable sandbox preparation, BLOCKED; build or runtime behavior contradicting expected result = FAIL. Do not switch to production.
5. Before GUI mutation, record baseline exclusively inside the test project: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml exec -T osjs-shell sh -c 'cat /data/config/simulator/devices.yaml; cat /data/config/mma2/config.yaml; sha256sum /data/config/simulator/devices.yaml /data/config/mma2/config.yaml'`; expect only `devices: []` and `listeners: []`. Confirm via project `ps`/port mapping the host exposes only the loopback UI, no Modbus host port. Never inspect operator files, host `/data` or production volumes.
6. Launch a FRESH real Chromium browser profile (test-only user data dir outside the repo) at `http://127.0.0.1:18219`. Open OS.js Start menu and click `MCS Modbus Toolkit` once; observe exactly ONE Toolkit window. In its ShadowRoot Memory tab, record screenshot and DOM-observed state: empty canonical Devices list, `No devices configured. Choose Add.`, no `Fixture Sim-PLC-1` or fixture-only Memory status, no automatic apply/write, Replicator/Diagnostics still fixture-only and actions disabled. Confirm baseline simulator document SHA/content unchanged before clicking Save & Apply. Capture actual browser JS errors (if any) and network/WebSocket observation; no substitute mock UI or unit tests.
7. In that GUI only: click Memory `Add`, set Name exactly `VERIFY-SIM-1`, Listen Port exactly `15020` (NOT default 5020), Unit ID `1`, leave FC1–FC4 Start 0 Count 16, all four simulation modes None / interval 0. Observe validation empty, click `Save & Apply` ONCE. Record screenshot immediately before and after, actual UI success or error, test Simulator/MMA2 logs, canonical test-only `devices.yaml`, effective `config.yaml` and `owners.yaml`, last apply path/acknowledgment if observable through existing UI/logs. Expect accepted `mma2-structural` apply, retained synthetic device and test MMA2 listener 15020 only inside its namespace. Reload/reopen Toolkit and verify canonical `VERIFY-SIM-1`, port 15020/unit 1/None intervals remain. Selected-device status must come from runtime evidence (MMA2 RUNNING and Simulation IDLE if observed after readiness), never turn UNKNOWN/UNAVAILABLE into falsely healthy. Record direct runtime status if available without any mutation or new endpoint.
8. Still ONLY on the test device, change FC3 Simulation from None to Random in the Memory GUI (default positive interval 1000 ms), click `Save & Apply` ONCE; capture UI and test-only canonical file/status evidence. Expect `random-runtime` timing path, persisted FC3 positive interval; FC1, FC2 and FC4 remain zero. Then switch FC3 back to None, click `Save & Apply` ONCE, reload window, and verify persisted FC3 interval 0, the others 0, and no false claim of active random ingest for an all-None device. Do not change any other port/identity or add any other device; maximum three explicit apply clicks total. If the UI/backend contradicts any required expectation, FAIL and proceed directly to owned-resource cleanup, not an unapproved repair/retry.
9. To observe runtime outage safely, stop ONLY the test Simulator service: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml stop modbus-simulator-runtime`; do not stop MMA2 or production anything. With Toolkit window open, observe its selected device transition to UNAVAILABLE or an explicit runtime-status error after a bounded poll/timeout, NEVER remain falsely RUNNING/IDLE indefinitely. Close/reopen only the test Toolkit: its load should show explicit Simulator runtime unavailable and MUST NOT substitute fixture values as canonical. Record screenshot/error. Restart ONLY the same test Simulator service with `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml start modbus-simulator-runtime`; wait up to 90 seconds for its Unix socket, reopen Toolkit, and confirm canonical `VERIFY-SIM-1` plus truthful observed selected status return. No Save & Apply during outage.
10. Record final test-only config/content/hash, logs, real-browser screenshots, exact Docker project/daemon identity, browser errors, observed UI states, request responses if visible and `git status --porcelain` BEFORE editing report (must be tracked-clean). Record whether Replicator/Diagnostics remain fixture-only; do not test their backends. Close the browser/test app; avoid writing credentials to the repo.

MANDATORY SAFE CLEANUP (even after FAIL/BLOCKED if test resources were created): First `sudo docker ps -a --filter "label=com.docker.compose.project=$PROJECT"` and corresponding volume/network label listings; inspect exact project identity. ONLY if every targeted resource is verified as belonging to the recorded disposable project run `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml down --remove-orphans` (NO `-v`); record exits and post-cleanup project container/network absence. Leave the exact test-only `verify-data` volume intact, record its full name and project label. Do NOT remove even that volume without separately approved authority; never use `docker compose down -v`, general prune, wildcard deletes, host services or production stack commands. If ownership cannot be proven, STOP destructive cleanup, report what remains and BLOCKED. Record the final tracked status; any unexpected tracked mutation -> report paths, do not restore/clean.

EXPECTED / VERDICT: PASS only if all required isolation, test startup, actual browser Memory load/three deliberate applies, canonical persisted data and truthful status, outage/recovery, evidence and bounded cleanup are directly observed. An executed product behavior contradicting an expected result = FAIL; necessary observation or safe environment unavailable after reasonable setup = BLOCKED. The initial missing-file `devices:null` representation is not covered because the verified test fixture explicitly seeds `devices: []`; report that limitation rather than claiming production empty-file correctness. No Docker/production acceptance, launcher cutover, Replicator backend or legacy UI retirement inferred.

EVIDENCE TO RETURN: complete HEAD/ancestor/sole-ACTIVE and daemon/project/preflight outputs; command/exit logs, image/startup/health/socket proof, before/after test-only config bytes or hashes, at most three apply click responses and classifications, screenshots of initial/edited/saved/status/unavailable/recovery, real-browser errors and relevant OS.js WebSocket observations, exact synthetic-only listener/network scope, project-labelled cleanup and retained test volume, final tracked status, failure classification and any unexpected behavior. Report which observations came from browser versus server logs; do not fill evidence gaps with assumptions or unit-test results.

REPORT-WRITE AUTHORITY: JR may replace ONLY `## JR TEST REPORT — UMIG-004-V` immediately below (up to `## Next action and recommendation`) with its factual independent PASS/FAIL/BLOCKED and raw evidence. Commit and push ONLY `handoff.md`, then STOP. No CODE, Compose, Active Work, archive, ICC, other handoff edits, follow-up tests or successor selection. ChatGPT reviews evidence and alone decides archival/UMIG-005 advancement or a separately approved bounded CODE repair.

## JR TEST REPORT — UMIG-004-V

PENDING — executable live VERIFY packet issued after corrected target preflight. No test containers started under this packet, no live browser/backend VERIFY performed, and no PASS/FAIL is predetermined.

## Next action and recommendation

Next action (OpenHands/JR): invoke `OPERATION CWAL` on latest `main`, execute this exact LIVE VERIFY packet within the disposable OpenHands sandbox, report and STOP. Recommendation: use the existing sandbox-local Docker daemon, never the operator's; on an actual failure preserve evidence and stop, with only project-owned cleanup permitted.
