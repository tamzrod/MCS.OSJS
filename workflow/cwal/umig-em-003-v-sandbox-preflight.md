# UMIG-EM-003-V — CWAL subordinate preparation commands

**Not an entry point.** OpenHands is invoked ONLY with `OPERATION CWAL`. The sole current `JR TEST TASK` in `handoff.md` incorporates steps 1–6 below as its exact commands and separately authorizes a handoff-only report/commit/push. Do not invoke this document directly or treat preparation as live VERIFY PASS. Use the existing disposable OpenHands environment; no new sandbox or clone. Prior evidence: fresh checkout was established at `f58033e`; Docker client was present but `/var/run/docker.sock` and daemon absent. Do not re-provision to rediscover that known fact.

## Exact preparation, once, in order

1. From the existing clean OpenHands repo (prefer `/tmp/em003v-discovery-clone` if present), read `operation cwal.md`, `handoff.md`, this file and `workflow/active_work/umig-em-003-v-shared-lock.md`. Require sole ACTIVE task `UMIG-EM-003-V` and current CWAL packet. Capture `pwd -P`, `git status --porcelain --untracked-files=all`, `git branch --show-current`, `git rev-parse HEAD`, `git rev-parse origin/main`, `git ls-remote --exit-code origin refs/heads/main`. On clean local `main` only, if HEAD is stale, explicitly authorized setup ONCE: `git fetch origin main`, then `git merge-base --is-ancestor HEAD origin/main` (must exit 0), then `git merge --ff-only origin/main`. Reread CURRENT handoff and subordinate packet after fast-forward; repeat status/HEAD/origin/main/live-remote check, require clean and exact equality. Also `git merge-base --is-ancestor 538324a472eb15ff8ef97ee66f826fa02ba9462f HEAD` must exit 0. If dirty, detached, diverged, still stale or packet changed scope, BLOCKED/STOP; no reset/rebase/clean/force/new clone or second fetch. Do not disclose credential-bearing origin URL.

2. Capture `uname -s`, `docker context show`, `docker context ls`, `env | grep -E '^(DOCKER_HOST|DOCKER_CONTEXT|COMPOSE_PROJECT_NAME|MCS_VERIFY_OSJS_PORT)=' || true`, `docker version`, `docker info --format '{{.Name}} {{.DockerRootDir}}'`. Require OpenHands disposable Linux sandbox and default LOCAL Unix Docker socket, not a remote/operator host. If daemon already exists and is sandbox-local, do not restart. If absent as previously reported (no socket and no dockerd/containerd), exactly ONE sandbox tooling startup is authorized: `test ! -S /var/run/docker.sock && ! pgrep -x dockerd && test ! -e /tmp/mcs-em003v-dockerd.pid && sudo -n sh -c 'nohup dockerd > /tmp/mcs-em003v-dockerd.log 2>&1 < /dev/null & echo $! > /tmp/mcs-em003v-dockerd.pid'`. Then `timeout 90s sh -c 'until sudo -n docker version >/dev/null 2>&1; do sleep 2; done'`; capture `sudo -n docker version`, `sudo -n docker info --format '{{.Name}} {{.DockerRootDir}}'` and context. On failure, read only `/tmp/mcs-em003v-dockerd.log` (redact sensitive output), report BLOCKED; no second attempt, installs, daemon config change, alternative host or further privileged action. This `sudo -n` exception covers only startup and read-only Docker metadata/inventory. No application containers are started in preparation.

3. Read only `deploy/verify/compose.yaml`, `deploy/verify/README.md`, `deploy/docker-compose.yml` (production file never executed). Capture `docker compose -f deploy/verify/compose.yaml -p mcs-em003v-discovery config` stdout/stderr/exit; capture `ss -ltn` if installed. Confirm internal runtime network/namespace, new project-scoped volume, seeded empty-only configs, no fixed names/privileged/host networking/operator mounts or public Modbus/RBE ports, and only loopback OS.js bind with free port. Unsafe or invalid => BLOCKED; no `compose up/build/down`.

4. Capture read-only inventory via `sudo -n docker ps -a --format '{{.ID}} {{.Names}} {{.Labels}} {{.Ports}}'`, `sudo -n docker network ls --format '{{.ID}} {{.Name}} {{.Labels}}'`, `sudo -n docker volume ls --format '{{.Name}} {{.Labels}}'`, `sudo -n docker compose -f deploy/verify/compose.yaml -p mcs-em003v-discovery ps -a`. The discovery project is NOT the later executable project's name. Record any collisions; do not inspect old volume contents, delete/reuse prior volumes or change any resources. An operator/shared daemon or unexpected conflicting project => BLOCKED.

5. Bounded source-read only: `simulator/runtime_server.go`, `simulator/apply.go`, `replicator/runtime_api.go`, relevant runtime Unix transport and OS.js Toolkit relay and MMA2 restart acknowledgment source. Record exact on-disk socket paths, length-framing and JSON `load`/`apply` schemas, restart/ack paths and uncertainties needed for one bounded synthetic parallel test. Do not invent requests, send calls, create scripts, edit code or run tests. Reuse previously reviewed source facts rather than repeat unrelated discovery.

6. SAFE POST-CHECK (also after any failure where feasible): capture `git status --porcelain --untracked-files=all`, `git rev-parse HEAD`, `git ls-remote --exit-code origin refs/heads/main`; identify daemon-start side effect and any unforeseen resource change. If clean/current and source-verified, complete the `handoff.md` report-only write, diff/scope checks, one non-force push and remote confirmation EXACTLY as authorized in the CURRENT handoff. Report all commands/exits and actual `PREPARATION COMPLETE`, `BLOCKED`, or `INCOMPLETE` as appropriate, with pushed report SHA if delivered; STOP. If checkout is dirty or remote advanced, do not commit/push; return real evidence in chat. Do not clean or tear down the daemon. No other repository writes, application startup, live test, task advancement or standalone report file.

Live cross-process verification remains separately gated. Only the coding agent issues its exact later packet; this preparation never establishes VERIFY PASS or promotes EM-004.
