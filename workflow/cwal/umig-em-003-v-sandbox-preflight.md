# UMIG-EM-003-V — OpenHands disposable sandbox read-only discovery

Authority: human selected a NEW disposable OpenHands sandbox on 2026-09-20. This is a PREPARATION / SAFETY DISCOVERY packet, **not** the executable cross-process VERIFY and **not** authority to start/stop services, create Docker resources, edit product files, generate a fresh test deployment or perform concurrent applies. The coding agent must pin actual test request bodies, endpoints, source revision, attempts, state restoration and scoped cleanup in a subsequent handoff after reviewing discovery evidence.

## Exact read-only discovery actions, in order

1. In a newly allocated OpenHands disposable workspace, read `handoff.md`, `operation cwal.md`, this file and `workflow/active_work/umig-em-003-v-shared-lock.md`; confirm they select the same sole ACTIVE task. `git status --porcelain --untracked-files=all`; `git rev-parse HEAD`; `git rev-parse origin/main`; `git ls-remote --exit-code origin refs/heads/main`; `git worktree list --porcelain`. No fetch, checkout, merge, reset or cleanup by JR. If stale, dirty or ambiguous, report BLOCKED and stop.
2. Record `pwd -P`, `uname -s`, `docker version`, `docker info --format '{{.Name}} {{.DockerRootDir}}'`, and `env | grep -E '^(DOCKER_HOST|DOCKER_CONTEXT|MCS_VERIFY_OSJS_PORT|COMPOSE_PROJECT_NAME)='` (redact secrets if unexpectedly present). Read-only only. If daemon is unavailable, report BLOCKED, do not start dockerd or install anything. Establish the Docker server is sandbox-local through observable daemon provenance; a client CLI or a worktree alone is not proof. If remote or shared daemon cannot be ruled out, BLOCKED.
3. Read the committed `deploy/verify/compose.yaml` and `deploy/docker-compose.yml`. Render ONLY the existing verify Compose via `docker compose -f deploy/verify/compose.yaml -p mcs-em003v-discovery config` (no up/build/create); save stdout/stderr and exit. Inspect for fixed container_name, host networking, privileged mode, bind mounts, externally named/reused volumes, public port binds, external/RBE outputs and mutable production paths. The verify manifest must retain internal runtime network, runtimes network_mode service:mma2, only loopback-published OS.js, seed refuses nonempty data; if not, BLOCKED. Do not render or operate production Compose beyond reading its file.
4. Read-only inventories `docker ps -a --format '{{.ID}} {{.Names}} {{.Labels}} {{.Ports}}'`, `docker network ls --format '{{.ID}} {{.Name}} {{.Labels}}'`, `docker volume ls --format '{{.Name}} {{.Labels}}'`, `docker compose -f deploy/verify/compose.yaml -p mcs-em003v-discovery ps -a` and `ss -ltn` (if available). Never assume discovery project name is authorized for execution; next packet will use a fresh unique token. Record port availability without binding it. Do not inspect volume contents, secrets or operator data. If any inventory requires elevated privileges, BLOCKED; no sudo.
5. Read-only inspect relevant Simulator and Replicator apply/restart source and Toolkit adapter(s) to identify exact existing local, non-network-exposing request mechanism, synthetic request schema, acknowledgement observability and safe bounded parallelism. Report concrete file paths/functions and input fields; DO NOT guess, send requests, run tests, write code, create configs, or use any endpoint. If unavailable, report that the coding agent must resolve it before executable VERIFY.
6. Capture final `git status --porcelain --untracked-files=all` and `git rev-parse HEAD`; confirm unchanged, report command exits, original outputs or full durable logs and all unsafe observations. Do not perform cleanup because this packet creates no resources. Return **DISCOVERY COMPLETE / BLOCKED**, not live PASS; STOP. Chat report only, no Git commit or push.

## Final executable VERIFY requirements (coding-agent responsibility; NOT authorized in this packet)

Source-pin a new activation SHA and live-main freshness gate; fresh random project token and exact named resources; sandbox-local daemon proof; clean inventory and loopback port; reviewed rendered config; bounded one-shot startup; baseline hashes of all three synthetic configs; exact synthetic Simulator and Replicator request bodies/endpoints, at most two applies each and explicit barrier/timing; capture individual replies/commit and restart acknowledgement timestamps; verify no lost update and foreign reservation retention; exact GUI/authorized reset to original hashes; label-verified `docker compose -p <token> -f deploy/verify/compose.yaml down --remove-orphans` without `-v`, retain volume, before/after inventories, failure cleanup boundary; independent raw evidence and optional specifically scoped handoff report transport. No test execution or task closure until separately published packet and safe target approval.
