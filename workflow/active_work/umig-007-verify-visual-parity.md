# UMIG-007 — VERIFY: Three-Tab Visual Parity

Status: QUEUED / DEFERRED — UMIG-006-V PASS; human now directs CURRENT Electron MCS Toolkit MODEL parity before visual-only acceptance.
Stage / owner: VERIFY / OpenHands JR; exact packet/review/advancement / ChatGPT.
Previous: UMIG-006-V (archived chronological predecessor only); new model-alignment tasks must be formally linked before activation.
Next: UMIG-007A (QUEUED; no cutover implied).

## Primary outcome
Compare the functionally aligned OS.js Toolkit appearance to the approved CURRENT Electron model at the same dimensions and comparable synthetic state. The old one-time visual donor `1c971b9a6e00bafadf329df8821421a40cfc079c` remains historical source provenance, not the new target.

## Changed prerequisites
Latest model assessed at `1ca174058548996913bdcc13c7e2ef224a768457`: Electron Memory layout and global MMA popup from `ff6846f`, newer Go Replicator measured COMMS telemetry from `1ca1740`. OS.js still lacks advanced per-device RBE/sealing/access-policy and shared MMA settings management, and its COMMS LEDs remain UNKNOWN pending verified fresh telemetry wiring. Earlier UMIG-006-V PASS applies only at its tested SHA and does not certify changed MMA2/Replicator code or model parity. First resolve `planning/microtask/umig-em-001-electron-model-contract.md` and independently implement/test/verify its authorized successors; no automatic CODE authorization.

Next obtain approved CURRENT-model Memory (definition/advanced/shared modal), Replicator and Diagnostics donor screenshots or an approved reproducible capture method pinned to current code, matched dimensions and safe state, with durable retrievable artifacts. The chat image with annotations shows only isolated Electron Memory advanced/RBE, not approved three-tab baseline. Historical `/tmp/jr-shots/` is not a durable approved donor reference. Further Electron changes require diff/re-pin.

## Future VERIFY action
Under a future exact `handoff.md` packet compare live OS.js tabs/navigation/spacing/forms, advanced and shared modal, selected devices, COMMS telemetry/UNKNOWN fallbacks, scrolling and shell/window/taskbar CSS isolation to the approved target. Windows-only service/path capabilities remain explicitly unavailable on Docker and are not to be faked. Compare appearance separately from live protocol and backend status; supply paired screenshots, dimensions, discrepancy list. Missing baseline or target => BLOCKED; concrete mismatch => FAIL for separately approved CODE fix.

## Non-scope / sizing
JR does not fix/debug source, contact production/customer devices, enable RBE network listener without authorization, edit ICC/CWAL, remove legacy UI or approve cutover. QUEUED not executable. Surface 0, environment 1, behavior 0, verification 1, recovery 1 = 3.
