# Handoff

## Current direction — 2026-09-18

Human confirmed that the Docker Compose deployment is working and explicitly requested the MCS Modbus Toolkit microtasks be promoted into Active Work. The target is a self-owned Toolkit inside the existing `osjs-shell` Docker image, reusing the existing MMA2, Simulator and Replicator services; Electron remains an independent Windows application. A prospective portable build and Modpoll tab are not authorized by this Toolkit sequence.

## Roles and task authority

ChatGPT owns CODE, donor provenance, source checkpoints, independent test review and workflow advancement. OpenHands/JR tests only an explicitly current JR TEST TASK via `operation cwal.md`, edits only its authorized report section and never codes or writes ICC. Only BLACK SHEEP WALL maintains ICC. `workflow/active_work/` and this handoff, not an outdated ICC summary, determine current work.

**Sole ACTIVE: UMIG-001 — human selection of one approved Electron UI donor SHA.** The user promoted the non-destructive UMIG-001 → UMIG-003 → UMIG-003-T → UMIG-003-V → UMIG-004 → UMIG-004-T → UMIG-004-V → UMIG-005 → UMIG-005-T → UMIG-005-V → UMIG-006 → UMIG-006-T → UMIG-006-V → UMIG-007 → UMIG-007A sequence on 2026-09-18. All successors are QUEUED in `workflow/active_work/`; each stage advances only after its own required evidence, and only the coding agent may change state. No JR TEST TASK is current while the human donor decision is ACTIVE. The donor SHA has **not** been selected, and the UI copying cannot begin until it is approved.

`DOCKER-001` is QUEUED/paused at the user's direction: the operator reports deployment working, but complete independent Linux/Windows build and transport evidence was not supplied; it is not marked PASS, archived, or inserted into the UMIG chain. Preserve Docker volumes and user configuration. Previously queued MEM/RLED/RREC/REP-BLOCK tasks remain unchanged. `UMIG-008` launcher cutover and `UMIG-009` legacy UI retirement and their tests remain in Planning pending separate explicit human approvals; they are not authorized for execution or automatic promotion. The legacy Simulator/Replicator UIs remain available throughout this sequence.

## Current microtask execution boundaries

- UMIG-001: human approves exact Electron renderer SHA; ChatGPT records source assets, renderer-only donor boundaries, Windows-only APIs and one-time-copy provenance. No donor guess or product edit.
- UMIG-003 CODE, TEST, VERIFY: self-owned Memory/Replicator/Diagnostics three-tab renderer inside the Toolkit, fixture build/discovery and rendered shell isolation. Keep missing status UNKNOWN and preserve desktop, Start menu and taskbar.
- UMIG-004 CODE, TEST, VERIFY: move Simulator bridge into Toolkit ownership and map Memory load/apply/status, then test fixtures and safely verify disposable live data. No production configuration writes.
- UMIG-005 CODE, TEST, VERIFY: move Replicator bridge into Toolkit ownership and map load/apply/status/suggest, then test fixtures and safely verify disposable live runtime. Preserve reservation ownership and truthful COMMS status.
- UMIG-006 CODE, TEST, VERIFY: Diagnostics mapping with unsupported Windows controls disabled, fixture tests and rendered safety observations.
- UMIG-007 and UMIG-007A: independent visual parity and electron-free Docker/OS.js deployment verification, then STOP for human cutover decision. Product builds/UI/backend tests are not pre-claimed by task promotion.

## Historical evidence and known limitations

Toolkit disconnected scaffold is already archived. UMIG-002-T package build/discovery independent PASS was reported at `e9d25e3d4c1b832126a161a6071fd4abf8b5546b`; UMIG-002-V one-window placeholder rendered PASS at `7349caace4762cedcef8109371fb8b02f7a0e8c2` and is archived. These do not prove functional tab integration or final migration. Original discovery FAIL remains recorded at `752a541`; prior ICC prerequisite BLOCKED remains at `3831e33`. Earlier icon and sound 404s remain observations, not fixes. DOCKER-001 source checkpoint is `1c971b9a6e00bafadf329df8821421a40cfc079c`; the operator's deployment report is recorded without inventing command outputs or Windows verification.

`ICC/INDEX.md` is known stale. Current Git task files and this handoff are authoritative; do not consume stale ICC as truth or automatically require BLACK SHEEP WALL before every coding/test step. BLACK SHEEP WALL alone may incrementally refresh affected context when actually needed.

## Next action and recommendation

Next action (human): select/approve the exact Electron renderer commit SHA to serve as the one-time three-tab UI donor; ChatGPT will record its source provenance, complete UMIG-001's decision gate, and only then activate UMIG-003. Recommendation: keep the working Docker deployment and persistent volume unchanged during UI source work; require independent TEST and VERIFY at each stage and return for separate approval before launcher cutover or legacy-app retirement.
