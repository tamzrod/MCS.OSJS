# UMIG-EM-002-V — VERIFY: New Go baseline in disposable stack
Status: PLANNED; no execution until human promotion and exact packet. Stage/owner VERIFY / OpenHands JR. Previous: UMIG-EM-002-T. Next: UMIG-EM-003.

Outcome: Confirm new Go/MMA2 and Replicator backend compatibility using one NEW owned empty project, not former verification volumes or production. Proposed actions: prove local Docker daemon and project labels, inspect `deploy/verify/compose.yaml`, reserve free loopback UI port, `docker compose --project-name <new-approved-project> -f deploy/verify/compose.yaml config`, then ONE `up -d --build`; prove healthz, Unix sockets, default RBE disabled/unpublished, baseline config hashes; use synthetic Memory source -> Replicator destination and backend v1 status to inspect new per-block evidence; verify ownership, no spontaneous apply and scoped teardown `down --remove-orphans` WITHOUT `-v` after ownership recheck. The eventual packet must replace the placeholder with exact unique token/commands and bounded actions before JR runs.

PASS requires real socket/backend observation and unchanged producer ownership; missing env BLOCKED, contradiction FAIL. No RBE listener publication, customer data, source changes, retry-driven debugging or claim visual/production pass. Return raw commands, exits, snapshots and cleanup. Size 0/2/0/1/1=4; split environment preflight if exact packet cannot bound it.
