# Active work — approved FMT queue

Human-approved execution queue: `FMT_PROMOTION_QUEUE.md` (FMT-001–FMT-008); root `handoff.md` authorizes its continuous dependency-aware processing. The Current task NONE checkpoint means no packet is presently in flight, not that the approved queue is retired. On CWAL invocation inspect the queue and packet headers, select an eligible packet, checkpoint exactly one ACTIVE in packet/queue/handoff, execute and record evidence/outcome, then rescan until no eligible work remains. Honor all blocker, role, scope and delivery gates. FMT-005 still needs an exact separately approved build command and test environment.

`OTR_PROMOTION_QUEUE.md` and all OTR packets/evidence are historical and NON-EXECUTABLE even if still physically present pending safe archival. Never infer historical completion or delete evidence. If queue/handoff disagree or a packet is missing, report BLOCKED and STOP. Only BLACK SHEEP WALL edits ICC; no unauthorized production access, destructive cleanup, merge, force push or main push.
