# Handoff

## Human direction / model authority — 2026-09-19

The human explicitly changed course: **follow the current Electron MCS Modbus Toolkit MODEL**, not merely pixel-match the old one-time donor. Review baseline for this decision: Electron `ff6846f973ad973bd3270b0ff2d707ce5c2616c9`. The historical one-time renderer donor `1c971b9a6e00bafadf329df8821421a40cfc079c` remains an immutable record of the earlier adaptation, NOT today's functional feature target. Further Electron changes require source diff and explicit re-pin; never import Electron renderer/preload/IPC/process-control implementation or synchronize blindly. Go/MMA2 owns actual config validation, source/destination ownership and apply; OS.js owns an authenticated adapter and window-scoped UI. Windows-only facilities remain disabled/UNAVAILABLE on Docker unless a separate real capability is intentionally authorized.

ChatGPT owns analysis/CODE, small specific test-packet authoring, JR evidence review and workflow advancement; OpenHands/JR is TEST/VERIFY ONLY under general `operation cwal.md`, edits only its assigned handoff report and stops. Only BLACK SHEEP WALL edits ICC. No change to `operation cwal.md`, production Docker/`osjs-data`, operator configs, legacy applications, retained test volumes or Windows Electron by this change of direction.

## Preserved independent evidence

UMIG-006-V live Diagnostics/window-lifecycle PASS was reviewed and archived at `ec390c0540d5aa07d7f5ad7f404c62690fd090c3`; immutable JR report `f6b7549b2c31e4fcf22427d8dee00b181f4b7186`. It proves tested sandbox browser behavior including actual Toolkit close/relaunch, but NOT the newly merged MMA2/RBE code, advanced Memory UI, global MMA settings, Windows service control, production, visual parity or cutover. Compare `ec390c0..ff6846f`: three new commits include Electron and MMA2/Simulator/composer changes and an Electron-scoped ICC addition; source inspection is NOT Go test, new image acceptance or deployed compatibility. Preserve test-only retained volumes `mcsverify-1789784184-232_verify-data` (if present), `mcsverify-rep-005-20260919_verify-data`, `mcsverify-diag-006-20260919_verify-data`; no reuse/removal without distinct authority.

## Source-level change analysis and new course

Electron currently implements Memory Device Definition/Advanced Settings folder tabs; per-device RBE Rules, State Sealing and Access Policy; shared MMA Settings dialog with separate draft/save/discard for global `rbe`, `access_events`, `debug`; validation through an Electron-local bundled MMA2 binary and its IPC. Go Simulator/composer now includes per-device advanced persistence/inheritance, effective-config validation and RBE integration. Current OS.js Memory editor is basic FC/None-Random only; Simulator v1 runtime and Toolkit relay/contract allow only `load/apply/status`. There is no authenticated shared `mma-load/mma-apply` Go protocol or advanced Toolkit UI. Copying Electron filesystem/Windows service calls or making the browser write YAML is PROHIBITED. RBE draft is not locked, has no TCP authentication/replay and no latency acceptance; do not enable or publish its output as a migration shortcut. Existing Memory/Replicator/Diagnostics verified behavior remains protected against regressions.

**Changed order:** (1) settle an explicit backend ownership/field/compatibility contract; (2) if approved, separate Go-owned shared-settings backend and full-config validation from Toolkit transport, with independent tests and disposable verification; (3) separately build per-device advanced editor preserving canonical fields, drafts, RBE IDs and validation, then independently test/verify; (4) separately build shared MMA modal and apply flow, independently test/verify; (5) run new upgraded-MMA2 compatibility/regression gates and only then UMIG-007 three-tab visual parity using an approved current-model baseline; (6) UMIG-007A independent deployment. UMIG-008/009 launcher cutover/retirement remain separate explicit human approval gates. The precise implementation microtasks/ordering may be finalized ONLY through the contract task below; no speculative product changes or test PASS asserted by this plan.

## Workflow stop gate — NO ACTIVE / NO CURRENT JR TEST PACKET

UMIG-006-V archived COMPLETE/PASS. `workflow/active_work/umig-007-verify-visual-parity.md` remains QUEUED / DEFERRED because it would test the wrong model and lacks a retrievable updated baseline. NEW PLANNING ONLY: `planning/microtask/umig-em-001-electron-model-contract.md` defines the first source/architecture decision gate. It has NOT been promoted to Active Work or executed as a formal stage. UMIG-007A stays QUEUED. There are ZERO ACTIVE tasks intentionally; no JR test instructions exist. A human promotion and exact stage packet are required before invoking `OPERATION CWAL`; do not assume this planning text authorizes OpenHands to debug, implement or test.

## Next action and recommendation

Next action: human review/promote the bounded Electron-model compatibility contract task if approved; ChatGPT then author the precise CODE -> independent TEST -> live VERIFY microtasks and handle implementation. Recommendation: preserve current product and operator data; do not rerun the old visual-parity or Diagnostics packets against the upgraded backend or claim old passes cover new capabilities.
