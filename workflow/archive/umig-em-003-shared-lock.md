# UMIG-EM-003 — CODE: Shared Go writer serialization

Status: COMPLETE / SOURCE-ONLY checkpoint, 2026-09-20. This is NOT a Go TEST, runtime VERIFY or production PASS.
Stage / owner: CODE / ChatGPT. Previous: UMIG-EM-003-S (archived source-only). Next: UMIG-EM-003-T (QUEUED TEST at checkpoint).

CODE checkpoint `949d7a8eb328dfa66f8359ffe81cfe359b8db2b7`; current source was read back through GitHub and `b64846e..main` path inventory reviewed. Simulator prior stage `b9d6747` guards boot/load/compose, structural restart ack and document save, and direct writer entry points; Replicator now uses same host-volume Linux `mma2composer.WithWriterLock` through manager boot/apply, composer load/owner resolution/commit/restart/ack/doc save, public standalone composer, document/legacy saves and RunOnce destination composition. Polling and starting/stopping pollers remain outside shared transaction; only necessary readiness/ack network checks occur within it. A committed-but-unacknowledged Replicator restart returns recovery-required error and does not rearm stale pollers. Added Linux two-producer collision, Simulator/Replicator busy-lock, and Replicator missing-ACK regression source; existing primitive crash-release test source retained.

Reviewed changed product paths relative to the REP CODE activation: `mma2composer/writer_lock_conflict_linux_test.go`, `replicator/compose_document.go`, `replicator/cycle.go`, `replicator/document_store.go`, `replicator/manager.go`, `replicator/store.go`, `replicator/writer_lock.go`, `replicator/writer_lock_test.go`, `replicator/writer_recovery_test.go`. Only handoff in addition. No source test was executed, no Docker/operator data altered, no ICC edit. Linux lock serializes cooperating processes sharing the same data root; it is advisory, not authorization, a filesystem transaction, or proof of full shared-manager/revision-CAS behavior. Go TEST is mandatory before any PASS claim or live VERIFY promotion.
