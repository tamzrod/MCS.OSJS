# Brainstorm Topics (Current Scaffolding Stage

Baseline commit: cb869da3caeed040478e67ecb0a01c5f93b3a66b
Working tree: clean
Source dependencies: planning/Brainstorm/port-config-and-socket-deployment.md, planning/Brainstorm/osjs-base-webapp-init.md

## Topic: Port Configuration and Socket Deployment (brainstorm, non-authoritative

Frames socket/port questions prerequisite to program architecture stage 4: which components expose sockets vs connect outward; port assignment/validation/persistence; who owns port config authority(c central or per-component); bind lifecycle semantics(f bind fail/restart/rebind); internal sockets vs container ports mapping external exposure; tcp/udp/unix vs network sockets. Constraint input (human, brainstorm not a decision): MMA2 must operate on host network adapter, not merely container-internal network, because it needs IP filtering. raises questions: host adapter scope/all-interfaces? passive(observe/mirror) or active(intercept/forward)?? raw sockets/TUN/TAP/BPF/eBPF/iptables hooks differ in privilege/socket requirements; comms between host-network-namespace MMA2 and container-resident components?; OS privileges for single-container appliance?; which MMA2 sockets bind host vs container-internal (configurable?)does host-adapter requirement force non-"one-container" deployment shape (host network mode, privileged sidecar, host process)? Open questions: Modbus acquisition TCP/serial/both?; internal IPC network vs unix vs mix?; single central port authority vs per-component?; default port namespace + collision detection?; socket lifecycle ownership vs component lifecycle?; which ports exposed externally + mapping policy?; auth/TLS excluded or included from initial planning?. Candidates(not decisions): single central validated config source consumed at each component startup; fixed well-known defaults + appliance-level overrides. Out of scope: design choice, microtask decomposition, implementation authorization.



## Topic: OS.js Base Web App Initialization (brainstorm, non-authoritative

Frames: initialize OS.js base web app as appliance presentation shell from donor(Nameless SCADA, copy color schemes + sample app, assign random port for OS.js management/UI endpoint, document that port. Staged alignment: touches donor inventory( stages 2-3, UI planning(stage 5); preparation for later microtask formulation; does NOT authorize copying or code writing. Licensing harvest gate applies before any copying(b see donor-licensing context;; sample app/color schemes/themes/icons/fonts/sounds each need own provenance audit(docs/LICENSING.md, record donor additions in THIRD_PARTY_NOTICES.md before import. Scope candidates: minimal OS.js web app shell from donor;( shipping sample app as-is reference vs structural/pattern template(b licensing impact, retained amount;; color schemes/theme config identity(b what is copied + licenses;; random port assignment for management/UI endpoint;; document chosen port(candidate homes docs/NETWORK_EXPOSURE.md and config/README reference;; record provenance/notices. Port dynamics questions: random once-at- scaffolding-then-documented-default vs random-each-boot-dynamic;; who selects + availability validation(ephemeral range, other appliance ports, host ports;; interplay with port authority question;; documented port leans one-time-select-then-document model(c open. Open questions: exact donor path/commit; sample app/color schemes as-is vs adapted (+per-asset notice); OS.js version/packages + transitive dep audits; base web app = frontend shell only or already includes backend stem(e.g., Orchestrator wiring;(keep responsibility separation explicit;; random port model(b above;; where documented port lives;, how random port validated against collisions. Constraints: NETWORK_EXPOSURE.md one OS.js endpoint + N MMA2 endpoints;; licensing harvest gate(()stop at boundary until provenance. Candidates(not decisions): vendor donor-derived base into controlled project location with provenance manifest; pick random port once at scaffold time, validate vs known port landscape, record as documented default alongside directive's one-OS.js-port rule; keep base minimal, sample/color schemes captured as reference/theme material(n not shipped as product UI. Out of scope: design choice, microtask decomposition, code writing, resolving licensing by assumption( stop until provenance established.