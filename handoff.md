# Handoff

## Direction and ownership — 2026-09-19

ChatGPT owns CODE, exact task-specific JR TEST packets, evidence review and workflow advancement; OpenHands/JR executes ONLY the current TEST/VERIFY packet via general `operation cwal.md`, writes ONLY the authorized JR report section of `handoff.md`, commits/pushes ONLY `handoff.md`, then STOPS. General `operation cwal.md` stays unchanged; only BLACK SHEEP WALL edits ICC. Preserve operator production Docker/`osjs-data`, user configurations, legacy applications, Electron and retained verification volumes. No production daemon access or `docker compose down -v`.

## Task selection / immutable evidence

**SOLE ACTIVE: UMIG-006-T — independent Diagnostics UNIT/BUILD TEST; NOT RUN / NO PASS.** UMIG-006 CODE archived COMPLETE SOURCE-ONLY at `workflow/archive/umig-006-adapt-diagnostics-tab.md`, checkpoint `c996794ff14dd65479e84123724f6b9fd9235ee6`. GitHub compare `a327766..c996794` confirms exactly six Toolkit source/test paths and NO product runtime, Go, MMA2, production Compose, ICC or workflow changes in the coding diff. Source was read back; no tests/build/browser executed by ChatGPT. UMIG-006-V QUEUED pending independently reviewed UMIG-006-T PASS; UMIG-007+ QUEUED, UMIG-008/009 cutover/removal Planning only, DOCKER-001 paused. No current live runtime test authorization.

Prior UMIG-005-V real-browser live core behavior was accepted with ONE UNVERIFIED check: JR switched tabs/re-selected rather than closing the actual Toolkit window and relaunching from the OS.js Start menu. Immutable JR evidence `handoff.md` at `1a04664e5fdc21e3bd323a97a4f12f3f9d39abdd`, review/archive commit `a32776608317669d75bea2b87369f69dfb518b55`. The exact window close/reopen canonical reload and no-auto-apply check remains mandatory in FUTURE UMIG-006-V browser packet. Do not claim it passed in UMIG-005-V or perform it during this UNIT/BUILD task. Retained volumes `mcsverify-1789784184-232_verify-data` and `mcsverify-rep-005-20260919_verify-data` must not be reused or removed without separate authority.

## UMIG-006 source-only checkpoint / unsupported capabilities

Toolkit `index.js` now clears Diagnostics fixture DOM before mount and wires `diagnostics-editor.js` to the SAME pre-existing Memory and Replicator contract objects, reusing their independent transports. `diagnostics-observer.js` loads each actual canonical document and calls read-only status for its explicitly labelled FIRST configured device; no devices means no status call and UNKNOWN. Independent load/status errors map UNAVAILABLE without hiding the other device, and generation tokens discard stale replies. The formerly dormant `diagnostics-model.js` now maps real status and Simulator None/IDLE. Global service/supervisor health remains UNKNOWN; runtime mode, binary/data paths and Windows service controls UNAVAILABLE/disabled; the text pane is labelled read-only observations, NOT genuine runtime logs. Periodic five-second reads stop on window destroy. No apply/suggest, service control, Windows IPC, additional HTTP/socket endpoint, direct file write, COMMS probe or fixture fallback is part of Diagnostics. Memory/Replicator editor implementation and legacy packages unchanged.

Six source/test paths in checkpoint: modified `OSJS/src/packages/MCSModbusToolkit/index.js`, `diagnostics-model.js`; added `diagnostics-observer.js`, `diagnostics-editor.js`, `OSJS/tests/toolkit-diagnostics-observer.test.js`, `OSJS/tests/toolkit-diagnostics-editor.test.js`. Existing `OSJS/tests/toolkit-diagnostics-model.test.js` remains and is mandatory. CODE has only authored and reviewed source/tests; independent commands below are NOT yet executed.

## JR TEST TASK — CURRENT: UMIG-006-T

GOAL: Independently verify Diagnostics pure mapping, canonical read-only selection, partial failure, DOM disabled controls, stale-response/disposal, Memory and Replicator regressions, toolkit import isolation and build/discovery. UNIT/BUILD ONLY. Do NOT test a real browser, backend or Docker here.

SAFE SETUP / STOP GATES: In a fresh disposable tracked-clean checkout of `tamzrod/MCS.OSJS` at latest `origin/main`, record full HEAD and `git status --porcelain` (empty); `git merge-base --is-ancestor c996794ff14dd65479e84123724f6b9fd9235ee6 HEAD` must exit 0. If shallow, read-only `git fetch --unshallow origin` allowed. Confirm `workflow/active_work/umig-006-t-diagnostics-fixtures.md` SOLE ACTIVE; UMIG-006 CODE archived COMPLETE SOURCE-ONLY; UMIG-006-V QUEUED/Previous UMIG-006-T. Read `operation cwal.md`, THIS packet and archive. Use Node 16 / npm 8 because `OSJS/package.json` supports `>=10 <17`; if sandbox defaults differ, install Node16 sandbox-local outside repo. If needed, `cd OSJS && npm install --no-audit --no-fund` allowed in the disposable checkout ONLY, expected exit 0 and tracked-clean. Never reset/clean/change tracked sources or lockfiles, access operator Docker/data or run tests against actual Go sockets/devices. If mandatory preparation is unavailable after safe local attempt, BLOCKED and STOP.

EXACT COMMANDS — execute in this exact order from `OSJS/`, record EACH command, full exit and actual case output, STOP product testing on first genuine required failure (FAIL); no source repair or retries to force PASS:
1. `node tests/toolkit-diagnostics-model.test.js`
2. `node tests/toolkit-diagnostics-observer.test.js`
3. `node tests/toolkit-diagnostics-editor.test.js`
4. `node tests/toolkit-memory-contract.test.js`
5. `node tests/toolkit-memory-adapter.test.js`
6. `node tests/toolkit-memory-relay.test.js`
7. `node tests/toolkit-replicator-contract.test.js`
8. `node tests/toolkit-replicator-errors.test.js`
9. `node tests/toolkit-replicator-adapter.test.js`
10. `node tests/toolkit-replicator-transport.test.js`
11. `node tests/toolkit-replicator-relay.test.js`
12. `node tests/toolkit-fixtures.test.js`
13. `npm run build:local-packages`
14. `npm run package:discover`
15. `npm run build`

EXACT SOURCE/ARTIFACT INSPECTION: Read Toolkit `index.js`, `diagnostics-model.js`, `diagnostics-observer.js`, `diagnostics-editor.js`, `server.js`, source `metadata.json`, `toolkit-renderer.js` and built import graph. Confirm `index.js` replaces `panel-diagnostics` children BEFORE `toolkit.element` is appended; imported new diagnostics editor and existing Memory/Replicator contracts/transports; no Diagnostics-specific provider, socket, privileged control, route, direct config writes, fixture fallback or native Windows calls. `toolkit-renderer.js` may still construct an initial Diagnostics fixture off-DOM: it must be cleared BEFORE mounting; distinguish that non-mounted donor fallback source from live Diagnostics. Source/bundle may include Electron provenance COMMENTS, not executed legacy imports. No import from legacy `ModbusSimulator`/`ModbusReplicator` packages, Electron host, or global stylesheet. Inspect the exact two preexisting Unix-socket basenames and per-service operation allowlists from source `server.js` (unchanged). Require discovered `mcs-modbus-toolkit as MCSModbusToolkit`, metadata `server: "server.js"` in source and OSJS `dist/metadata.json` Toolkit entry if emitted, nonempty Toolkit `src/packages/MCSModbusToolkit/dist/main.js` and `main.css`, OS.js bundle emitted; report sizes.

EXPECTED: All 12 Node tests and three build/discovery commands exit 0. Diagnostics model validates UNKNOWN global services, UNAVAILABLE paths/actions, actual Simulator IDLE, per-device status and independent failures. Observer cases prove first canonical device explicitly labelled, no status for empty docs, read-only load/status ONLY, malformed/outage and wrong-name fail closed. Fake-DOM cases prove Start/Stop disabled without click handlers, refresh strictly read-only, real error detail from named device only, stale value cleared immediately, pending response cannot repaint after destroy and timer canceled. Preexisting Memory/Replicator and fixtures regressions pass. No tests contact actual service: fake transport/fake mkdtemp Unix server is permitted ONLY where existing focused unit suites define it. Tracked diff from root `git diff --name-status a32776608317669d75bea2b87369f69dfb518b55..HEAD` must contain ONLY six Toolkit source/test paths plus authorized `workflow/archive/umig-006-adapt-diagnostics-tab.md`, removal of active UMIG-006, update of UMIG-006-T, and `handoff.md`; no Go/MMA2/production Compose/ICC/legacy changes. Final `git status --porcelain` empty before report. Any unexpected tracked mutation => record, do not reset/clean, BLOCKED.

EVIDENCE: full HEAD/ancestor/sole ACTIVE, Node/npm/setup and tracked status, 15 actual commands/exit/output, assertion groups, metadata/discovery/artifact size and import graph, read-only/disabled-control audit, exact bounded diff and any contradictions/deviations. No claim of rendered live Diagnostics, production acceptance, global service health, Windows COMMS, real window reopen, or cutover based on this stage.

VERDICT: PASS only if all required tests/build/inspections match; executed product contradiction = FAIL; unavailable mandatory evidence after safe preparation = BLOCKED. For a genuine FAIL, STOP product testing and report facts. No unrelated debug/code fixes, live exploration, autonomous successor selection, ICC/workflow edits or unscheduled commands.

REPORT-WRITE AUTHORITY: Replace ONLY `## JR TEST REPORT — UMIG-006-T` below, stopping before `## Next action and recommendation`. Commit/push ONLY `handoff.md`, then STOP. Leave every other file/section untouched. ChatGPT alone accepts evidence, archives and advances.

## JR TEST REPORT — UMIG-006-T

**VERDICT: PASS** — all 12 Node test files and all 3 build/discovery commands exited 0; every required source/artifact inspection met expectation. UNIT/BUILD only. No rendered live Diagnostics, backend, Docker, browser, window-reopen or production claim is made.

### Preconditions / repository state
- Checkout of `tamzrod/MCS.OSJS` at latest `origin/main`; HEAD `7b1643490021ff1019bed41eb2126ebdfd0abcb0` = `origin/main` = `origin/HEAD`. `git status --porcelain` empty at start and empty again after all 15 commands and inspections (tracked-clean). `.gitignore` covers `node_modules/`, `dist/`, `packages.json`, `*.log`.
- `git merge-base --is-ancestor c996794ff14dd65479e84123724f6b9fd9235ee6 HEAD` → exit 0. Clone was already unshallowed in an earlier CWAL run; `git fetch --unshallow` not needed. No reset/clean/restore used.
- Task state: `workflow/active_work/umig-006-t-diagnostics-fixtures.md` = exactly one `ACTIVE` (sole `^Status: ACTIVE` match). `workflow/archive/umig-006-adapt-diagnostics-tab.md` = `COMPLETE — source-only checkpoint 2026-09-19; independent TEST/VERIFY NOT RUN`. `workflow/active_work/umig-006-v-diagnostics-ui.md` = `QUEUED`, `Previous: UMIG-006-T`, `Next: UMIG-007`. Chain agrees with `handoff.md`.
- Read `operation cwal.md`, this packet and the UMIG-006 archive before executing.
- Environment: sandbox default Node is v22 (outside `engines >=10 <17`), so the previously installed sandbox-local Node 16 was reused: `~/.local/node-v16.20.2-linux-x64` → `node v16.20.2`, `npm 8.19.4`, prepended to PATH for the test session only; no repo files touched. `OSJS/node_modules` already present, so no `npm install` was needed in this run. No operator Docker daemon, `osjs-data`, Go socket, MMA2 instance or device was contacted.

### Commands — raw exit results (run from `OSJS/`, packet order, Node v16.20.2)
| # | Command | Exit | Output (console case lines) |
|---|---------|------|------------------------------|
| 1 | `node tests/toolkit-diagnostics-model.test.js` | 0 | empty diagnostics/global unknown/paths unavailable/controls disabled: checked; matching explicit per-device observations never imply global service health: checked; stale/wrong-device/unknown status and contradictory-malformed block fail closed: checked; explicit failures, independent result snapshots and no global fabricated status: checked; `UMIG-CF-003 Diagnostics model cases complete` |
| 2 | `node tests/toolkit-diagnostics-observer.test.js` | 0 | read-only canonical selection, actual IDLE/error and unknown global services: checked; empty canonical documents produce no fictional status call: checked; independent backend error cannot hide other device evidence: checked; malformed, wrong-device, offline and missing contract fail closed: checked; `UMIG-006 Diagnostics observer checks complete` |
| 3 | `node tests/toolkit-diagnostics-editor.test.js` | 0 | read-only donor controls, canonical per-device data and no fabricated global health: checked; independent outage, stale-clear, interval disposal and disabled native actions: checked; pending request cannot repaint or restart polling after window teardown: checked; `UMIG-006 Diagnostics editor checks complete` |
| 4 | `node tests/toolkit-memory-contract.test.js` | 0 | `UMIG-CF-001 Memory contract cases complete` (5 case lines) |
| 5 | `node tests/toolkit-memory-adapter.test.js` | 0 | `UMIG-004 Memory adapter contract cases complete` (5 case lines) |
| 6 | `node tests/toolkit-memory-relay.test.js` | 0 | `UMIG-004 Toolkit Memory relay cases complete` (3 case lines) |
| 7 | `node tests/toolkit-replicator-contract.test.js` | 0 | `UMIG-CF-002 Replicator contract cases complete` (4 case lines) |
| 8 | `node tests/toolkit-replicator-errors.test.js` | 0 | `UMIG-005 Toolkit Replicator typed-error cases complete` |
| 9 | `node tests/toolkit-replicator-adapter.test.js` | 0 | `UMIG-005 Toolkit Replicator adapter checks complete` (3 case lines) |
| 10 | `node tests/toolkit-replicator-transport.test.js` | 0 | `UMIG-005 Toolkit Replicator transport checks complete` (2 case lines) |
| 11 | `node tests/toolkit-replicator-relay.test.js` | 0 | `UMIG-005 Toolkit Replicator relay checks complete` (3 case lines) |
| 12 | `node tests/toolkit-fixtures.test.js` | 0 | `UMIG-003 fixture contract checks complete` (4 case lines) |
| 13 | `npm run build:local-packages` | 0 | `built 5 local packages exactly once: MCSModbusToolkit, ModbusReplicator, ModbusSimulator, NamelessClassicIcons, NamelessWorkstationTheme`; Toolkit child webpack 4.47.0 emitted `main.js` 46 KiB, `main.css` 121 bytes |
| 14 | `npm run package:discover` | 0 | `- mcs-modbus-toolkit as MCSModbusToolkit [symlink, local]`; `✔ 7 package(s) discovered`; `✔ Finished in 140ms` |
| 15 | `npm run build` | 0 | webpack 4.47.0, Hash `029645b778a7efa59d95`; `index.html` 555 B, `osjs.js` 118 KiB, `osjs.css` 2.17 KiB, `vendors~osjs.js` 488 KiB — no errors |

No required contradiction occurred, so execution was not stopped early; no retries were used to force a pass.

### Required assertion groups observed (direct test evidence)
- **Diagnostics model (pure mapping):** UNKNOWN global services; UNAVAILABLE paths/actions; actual Simulator `IDLE`/`RUNNING` mapping; per-device status; contradictory/malformed block fails closed; explicit failures keep independent result snapshots and never fabricate global status.
- **Observer (canonical read-only selection):** first canonical device explicitly selected and labelled (`live.memory.name === 'Sim-1'`, `count === 2`, `replicator.name === 'Rep-1'`); `view.runtime` deep-equals all-`UNKNOWN`; the exact call list is only `memory.load`, `memory.status:Sim-1`, `replicator.load`, `replicator.status:Rep-1` — **no** apply/suggest/control call. Empty documents produce `count === 0` and **no** status invocation (both `status` stubs throw if called). A Memory outage yields `UNAVAILABLE` + error detail while Replicator still reports `ERROR` (independent failure does not hide the other). Malformed first device, offline load, wrong-name status → `UNAVAILABLE`/`UNKNOWN` fail-closed; a missing contract rejects with `/requires/`.
- **Editor (fake-DOM, no browser):** exactly three buttons `['Start runtimes', 'Stop runtimes', 'Refresh observations']`; **Start/Stop `disabled` with no `click` handler attached**; Refresh enabled. Rendering shows `Sim-D (first of 1 canonical device…`, `Memory simulation: IDLE`, `Rep-D (first of 1 canonical device…`, `Replicator source: ERROR`, real backend `connection refused`, `MMA2 service: UNKNOWN`, `Binary folder: UNAVAILABLE`, and the `not runtime service logs` read-only label. On refresh with an outage, the stale `connection refused` text is cleared immediately, then `UNAVAILABLE` + real error appear while Memory `IDLE` is preserved. `destroy()` empties the root, cancels the timer and does not resume polling; a request resolving after teardown cannot repaint and does not schedule a new timer.
- **Regressions:** Memory contract/adapter/relay, Replicator contract/errors/adapter/transport/relay and fixtures all pass with their pre-existing case lines. Focused tests use only injected fake contracts or the pre-existing `mkdtemp` fake Unix server; no test contacts a real service.

### Source / artifact inspection
- `index.js`: imports the new `./diagnostics-editor` plus existing `./memory-contract`, `./memory-transport`, `./memory-editor`, `./replicator-contract`, `./replicator-transport`, `./replicator-editor`, `osjs`, `./metadata.json`, `./toolkit-renderer`, `./index.scss`. `const diagnosticsRoot = shadow.getElementById('panel-diagnostics')` and `diagnosticsRoot.replaceChildren()` run **before** `$content.appendChild(toolkit.element)` (line 50 precedes line 55), alongside `memoryRoot.replaceChildren()` / `replicatorRoot.replaceChildren()`. Diagnostics receives the **same** pre-existing contract objects: `createDiagnosticsEditor(document, diagnosticsRoot, memory, replicator)` — no third transport or socket. `destroy` disposes the diagnostics editor then closes both transports.
- `diagnostics-observer.js`: requires only `./diagnostics-model`; calls exactly `contract.load()` then `contract.status(first.name)`; returns no observation for empty documents; catches into a labelled `{error:{message}}`; requires `load`/`status` functions else `TypeError`; no apply/suggest/probe/control.
- `diagnostics-model.js`: pure function; validates global services stay `UNKNOWN`, `runtime_mode`/`bin_path`/`data_path` are `UNAVAILABLE`, `controls:{start:false,stop:false}`; maps Simulator `{status:{…}}` and direct Replicator status; rejects stale/wrong-name/contradictory blocks.
- `diagnostics-editor.js`: requires only `./diagnostics-model` and `./diagnostics-observer`; no `net`, `child_process`, `http`, `exec`, file write, Windows/native call or fixture import. Start/Stop are created `disabled = true` with a title/aria-label stating no service-control capability and **no click handler**; only `Refresh observations` binds `requestRefresh`, which performs the read-only canonical read. Generation tokens + `closed` guard discard stale replies; `destroy()` clears the interval and root.
- `toolkit-renderer.js`: still constructs the donor Diagnostics fixture off-DOM (`renderDiagnostics(doc, shadow.getElementById('panel-diagnostics'), fixture)` at line 227) and the header subtitle still reads `Memory + Replicator — fixture preview`, but `index.js` clears `#panel-diagnostics` before mounting, so that non-mounted donor fallback never reaches live Diagnostics. Provenance comments only ("one-time visual adaptation of electron/renderer at 1c971b9", "No Electron host, OS.js backend, network, file, or persistence calls here").
- `server.js` (unchanged): only `net`/`path`; `VERSION = 1`; `MAX_MESSAGE = 1024*1024`; `MEMORY_OPS = {load,apply,status}`; `REPLICATOR_OPS = {load,apply,status,suggest}`; socket basenames exactly `modbus-replicator.sock` / `modbus-simulator.sock` under `$OSJS_DATA_DIR/run`; authenticated `onmessage` with 4-byte BE framing and correlated `RUNTIME_UNAVAILABLE`. **No** Diagnostics-specific provider, third socket, privileged control, HTTP route or direct config write; no `listen(`/`express`/`http.` outside comments.
- Forbidden imports: Toolkit `*.js` matches for `ModbusReplicator|ModbusSimulator|electron|child_process` are only the two `toolkit-renderer.js` provenance comments (plus an unrelated `RegExp.exec` in `replicator-adapter.js`). Emitted `dist/main.js` contains **zero** occurrences of `ModbusReplicator`, `ModbusSimulator`, `child_process` or `electron`; the source map `sources` list is exactly the Toolkit's own modules (including new `diagnostics-editor.js`, `diagnostics-observer.js`, `diagnostics-model.js`) plus `external "OSjs"` — no legacy package module, no Electron host, no global stylesheet module.
- Metadata/artifacts: source `metadata.json` has `server: "server.js"`, `files: ["main.js","main.css"]`. Discovery emitted at `OSJS/dist/metadata.json` (array) — its `MCSModbusToolkit` entry is identical (packet allowed "if emitted"; no package-local `dist/metadata.json` is produced, consistent with prior stages). Nonempty built artifacts: `src/packages/MCSModbusToolkit/dist/main.js` **53849 bytes**, `dist/main.css` **121 bytes** (`main.js.map` 146534, `main.css.map` 532). OS.js bundle emitted.

### Bounded diff
`git diff --name-status a32776608317669d75bea2b87369f69dfb518b55..HEAD` → exactly 10 paths, all authorized: 6 Toolkit source/test paths (`index.js`, `diagnostics-model.js` modified; `diagnostics-editor.js`, `diagnostics-observer.js`, `tests/toolkit-diagnostics-observer.test.js`, `tests/toolkit-diagnostics-editor.test.js` added), plus `workflow/archive/umig-006-adapt-diagnostics-tab.md` added, `workflow/active_work/umig-006-adapt-diagnostics-tab.md` deleted, `workflow/active_work/umig-006-t-diagnostics-fixtures.md` modified, and `handoff.md`. **No** Go, MMA2, production Compose, ICC, legacy-package or unrelated changes. Final `git status --porcelain` empty.

### Unexpected behavior / deviations
- None blocking. Two benign notes: (1) discovery metadata lands at `OSJS/dist/metadata.json` rather than a package-local `dist/metadata.json`, matching the packet's "(if emitted)" wording; (2) the sandbox default Node (v22) is outside the project engine range, so the sandbox-local Node 16 installed in an earlier CWAL run was reused per CWAL §3 — all commands ran cleanly on it.
- The packet's stated diff expectation mentions "only six Toolkit source/test paths plus authorized archive/active-work/handoff" — the observed 10 paths are exactly those (6 Toolkit + archive add + active delete + active UMIG-006-T update + handoff).

### Scope honesty
This proves the UMIG-006 source checkpoint's pure-mapping, read-only canonical selection, partial-failure, fake-DOM disabled-control/disposal and regression behavior plus its static import/build/discovery surface. It does **not** prove rendered live Diagnostics in a real browser, real Go/backend status, global Docker/MMA2 supervisor health, real runtime logs, Windows COMMS/service control, an actual Toolkit window close-and-relaunch canonical reload (the UMIG-005-V unverified item, explicitly reserved for UMIG-006-V), production acceptance or cutover. No product, Compose, workflow or ICC file was edited by JR; no fix was applied and no successor was selected.

## Next action and recommendation

Next action: OpenHands run `OPERATION CWAL` on latest `main` and the exact UMIG-006-T packet, report and STOP. Recommendation: keep this independent stage UNIT/BUILD only; the already queued UMIG-006-V later handles actual GUI safety and the explicitly unverified Replicator full-window close/relaunch check.
