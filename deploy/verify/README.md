# UMIG-004-V disposable test target (PREPARED, NOT VERIFIED)

This is a test-only Compose definition; it is **not** an override of `deploy/docker-compose.yml` and must NEVER be combined with it. Source preparation is not proof of isolation, a running environment, or a Toolkit VERIFY PASS. OpenHands/JR must use a separately owned, disposable Linux VM/sandbox with its **own Docker daemon**, accessible browser and clear teardown authority. A second project on the operator's production Docker daemon is forbidden, even if the Compose project name differs. If a distinct Docker host/daemon cannot be demonstrated, report BLOCKED without starting anything.

## Intended isolation

- All four services (`seed`, `mma2`, `modbus-simulator-runtime`, `osjs-shell`) use **no `container_name`**, external Docker volume, production data bind mount, or host network. A unique project name creates only its own resources. `seed` refuses a nonempty data volume and initializes only `listeners: []` in the new `verify-data` volume.
- MMA2 runs only on `verify-runtime` (`internal: true`), with **no published Modbus port**. Simulator uses `network_mode: service:mma2` so its required `127.0.0.1` MMA2 readiness and Raw Ingest calls reach ONLY test MMA2 in the same private network namespace. It shares the disposable `/data` volume and Unix socket with test OS.js; no host socket is mounted.
- Test OS.js is on `verify-ui` (separate bridge) and publishes ONLY its UI at `127.0.0.1:${MCS_VERIFY_OSJS_PORT:-18219}:18209`; this is a default suggestion, not a permission to take an occupied port. Do not publish ports 5020/15020 or run `network_mode: host`.
- Do not mount, inspect, copy or restore production `osjs-data`, `/data`, MMA2 configurations, sessions, credentials or user files. No Replicator, Electron, external device or customer Modbus endpoint is included.

## Mandatory nonmutating preflight on the independent disposable host

1. Record hostname, Docker context/daemon endpoint, `docker info` server identity, who owns the VM, proof that it is NOT the operator/production Docker daemon, operator-visible containers and volumes on this daemon, and checkout HEAD. A claimed `COMPOSE_PROJECT_NAME` by itself is **not** proof. If any ownership is ambiguous, STOP / BLOCKED.
2. Ensure test checkout is tracked-clean; ensure Docker Engine + Compose v2 are available. Confirm production container names (`mcs-osjs-shell`, `mcs-mma2`, `mcs-modbus-simulator-runtime`, `mcs-modbus-replicator-runtime`) are absent on this daemon. Confirm the UI port is unused on this host; if occupied, STOP rather than stopping anything. Never run an action against an unidentified Docker context.
3. Choose and record a fresh unique test-only project identifier (e.g. `mcsverify-<unique-suffix>`; do not use `mcs`, `mcs-osjs` or the production project). Run only `docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml config` to inspect the resolved configuration before any `up`; check the exact build paths, absence of `container_name`, host networking and external volumes, private MMA2 network, loopback-only published port and scoped volume. Verify that this project has zero pre-existing containers, networks and volumes. If any invariant fails, STOP / BLOCKED.
4. Submit the recorded target and nonmutating preflight results to ChatGPT. ChatGPT must accept the exact target and publish a new **executable UMIG-004-V JR packet** before any startup, configuration write or browser operation. `OPERATION CWAL` under the present preflight-only handoff has NO permission to start the stack.

## After ChatGPT publishes an executable packet (NOT authorized yet)

Only on the verified disposable host, use the explicit unique `--project-name "$PROJECT"` and this file alone to build/start. Wait for test MMA2, Simulator socket and OS.js `/healthz` before opening a clean browser at the resolved loopback UI port. Start with the empty canonical Simulator document. Test ONLY synthetic named devices on isolated port `15020`, unit 1, never a customer IP/port; confirm source/destination boundaries before intentional Save & Apply. Confirm actual canonical reload/status and backend response, None/Random persistence, error/unavailable states and screenshots. A failed readiness step is FAIL/BLOCKED per the later packet; do not redirect a connection to production.

For teardown, stop only the explicitly identified test project with `docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml down --remove-orphans` after verifying project ownership. The named test volume is deliberately NOT removed automatically; enumerate the exact volume and inspect its `com.docker.compose.project` label before any separately approved `docker volume rm <exact-test-volume>` operation. Never execute `docker compose down -v`, generic `docker system prune`, blanket process kills, cleanup by wildcard or any production-stack command. Record remaining volume and other resources honestly if complete safe cleanup cannot be proven.
