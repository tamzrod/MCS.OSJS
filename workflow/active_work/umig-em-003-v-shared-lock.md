# UMIG-EM-003-V — VERIFY: Cross-process writer serialization

Status: ACTIVE — ONE-SHOT LIVE VERIFY packet issued in `handoff.md`; independent OpenHands JR has NOT executed it yet.
Stage / owner: VERIFY / OpenHands JR, invocation ONLY `OPERATION CWAL`. Previous: UMIG-EM-003-R-T archived Go race TEST PASS. Preparation independently completed in report commit `ebd835ecea7c4215b29effd62b5993cff1e8fe3c` (not live PASS). Next: UMIG-EM-004 stays PLANNED; promotion requires human approval after independently reviewed live VERIFY PASS.

Outcome: Demonstrate actual simultaneous synthetic Simulator/Replicator shared-MMA2 writer serialization with two preserved, distinct producer owners, no lost effective-config update, observed ordered restart request/ACK SHA pairs and truthful replies. Test only the existing disposable OpenHands sandbox with `deploy/verify/compose.yaml`. No new sandbox/clone, production Compose, operator host/device, external Modbus/RBE/public port or old volume.

CURRENT executable authority is exactly ONE `JR TEST TASK` in `handoff.md`, referencing fixed one-shot coding-agent-authored runner `workflow/cwal/umig-em-003-v-live.py` (blob SHA `36a2c3a4154b5caeb0cdbf5d974abb794465cb36`). The packet specifies clean fast-forward when needed, source-ancestry and live-main gates, sandbox-local daemon, unique fixed project/volume, preflight, at most one concurrent apply per runtime, per-producer API restoration, restart evidence, bounded timeouts, labels-only cleanup without `down -v`, retained test volume, full log and report-only push. The runner is not an independent OpenHands instruction; `OPERATION CWAL` is the sole entry point. Never rerun the one-shot test after product or environmental failure without a new human-authorized packet.

Acceptance: (1) proven simultaneous bounded synthetic applies in the sandbox; (2) both owner reservations, composed config and documents preserved with matching sequential restart request/ACK SHA observations and truthful responses; (3) exact baseline restoration where safe, evidence and test-only labelled project cleanup, no unrelated changes. Contradiction FAIL; unsafe/missing environment or required observation BLOCKED; incomplete safe cleanup/report delivery INCOMPLETE as specified by CWAL. Unit TEST or preparation result never substitutes for live VERIFY.

JR reports evidence only. Coding agent independently reviews actual report and archives on genuine PASS; no automatic next-task promotion, no JR product/workflow/ICC edits except exact handoff and test log report-only exception.
