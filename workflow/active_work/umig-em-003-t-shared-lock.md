# UMIG-EM-003-T — TEST: Shared writer lock (one OpenCode JR trial)

Status: ACTIVE — human-approved one-task OpenCode V2 JR exception; incomplete initial attempt superseded by explicit human-requested full rerun under corrected current packet. No adjudicated TEST PASS or report commit exists yet.
Stage / owner: TEST / independent OpenCode V2 on separate Legion JR worktree; ChatGPT owns coding, evidence review and task advancement. OpenHands fallback only, NOT runner for this trial.
Previous: UMIG-EM-003 (archived COMPLETE / SOURCE ONLY, product checkpoint `949d7a8eb328dfa66f8359ffe81cfe359b8db2b7`).
Next: UMIG-EM-003-V (PLANNED only; separate human approval and safe runtime target required).

## Primary outcome
Independently compile/test the pinned shared Linux lock and Simulator/Replicator integrations with the exact offline Go unit/fixture/concurrency/race suites, then complete the mandatory post-test checks and **push the sole `handoff.md` JR report** for coding-agent review. No production, Docker, sudo, operator Modbus endpoints, installed config or persistent services. Existing Go tests may use only their own synthetic ephemeral loopback fixtures.

## Why the current packet supersedes the first attempt
The first OpenCode session halted partway through preflight due to a local `steps: 8` setting; after removing it, the agent described the three suites as passing but halted again without independently provided full evidence, post-check and delivery. Its original packet prohibited report write/push and was inconsistent with the user's desired JR workflow. These observations are NOT an accepted TEST result. Human expressly requested updating this ACTIVE task, `operation cwal.md`, and handoff, then authorizing **one new full run** rather than repeated continuation prompts. Do not reuse or silently convert the old screenshots into PASS or claim that the old attempt delivered a report.

## Revised acceptance / authorized test boundary
1. The human updates the *clean, detached* `$HOME/apps/MCS.OSJS-jr` worktree to the **new remote activation commit**, reviews the pinned runner source and individually approves its invocation in OpenCode. JR checks HEAD, origin/main and read-only LIVE GitHub main with `git ls-remote`, pins the original product SOURCE SHA above, verifies the exact allowed workflow/runner-only diff and exactly this ACTIVE task; Go >=1.25, offline dependencies and >=3 GiB free disk. JR NEVER fetches/switches/merges/resets/cleans to repair checkout. Stale/ambiguous state => BLOCKED before tests.
2. Run the fixed, coding-agent-authored `workflow/cwal/umig-em-003-t.py` once under the exact handoff command. It deterministically handles A preflight → B three Go race suites (one attempt each, stop on first failed product gate) → C clean/unchanged post-check → required named PASS and full stdout/stderr → report-only `handoff.md` update → one-file Git commit/non-force main push/remote confirmation. A source/test FAIL must remain FAIL in the report; blocked environment cannot become PASS. The runner itself is source-only at activation, not a TEST PASS. Human approval of this named script authorizes ONLY its enumerated commands, synthetic Go temp/cache writes, read-only GitHub main lookup and report-only commit/push. No unattended `--auto`, `allow always`, arbitrary commands, installs or live endpoints. Worktree is NOT a security sandbox.
3. **Report-write/transport exception is expressly APPROVED for this sole task**: runner may replace ONLY the final `## JR TEST REPORT — UMIG-EM-003-T` section of `handoff.md` after a clean C, preserve all previous bytes, verify `handoff.md` is the ONLY changed/staged/committed file, commit it from detached HEAD and non-force push `HEAD:refs/heads/main` only if live remote still equals pre-test HEAD. It verifies remote equals report commit and the tree is clean, prints verdict+SHA and STOPS. No other script or shell redirection may bypass normal OpenCode edit DENY. If preflight, post-check, write, commit or push cannot safely finish, state BLOCKED/INCOMPLETE with exact reason and do not claim pushed report; a real underlying product FAIL must still be disclosed. The user's approval does NOT authorize product/source/workflow/ICC edits.
4. ChatGPT fetches the actual report commit, checks raw suite evidence, unexpected side effects and sole changed path, then and only then decides TEST acceptance and any next workflow promotion. A passed Go TEST is not a live VERIFY.

`handoff.md` is the sole exact JR execution packet and specifies the single approved runner command and expected SHA checks. This file does not authorize separate tests. OpenCode local profile keeps `shell: ask`, `edit: deny`, subagents denied and no `--auto`; *only* the reviewed runner's narrowly authorized report write runs through its human-approved shell invocation. Do not weaken global permissions. ICC only BLACK SHEEP WALL. Sizing 0/1/0/1/1=3.
