# UMIG-004-V — disposable Memory live-verification environment

Use ONLY `deploy/verify/compose.yaml`, never as an override of `deploy/docker-compose.yml`. This is sandbox test infrastructure, NOT a production deployment and NOT a verified result. Current accepted target class is the existing ephemeral OpenHands runtime with a newly started sandbox-local Docker daemon, as independently recorded in the JR preflight at `37dc61f533c8e7221610c8bf5858ddbe01790312`. A separate VM is NOT a prerequisite. The production Docker daemon, `osjs-data`, settings, ports, customer devices and production configurations are strictly out of scope. If the sandbox was recycled, re-establish fresh daemon/resource evidence under the current executable handoff packet rather than treating a missing socket as an automatic BLOCKED result.

## Why this Compose is separate

- Four services: isolated `seed`, `mma2`, `modbus-simulator-runtime`, and `osjs-shell`. No fixed container names, bind/production mounts, external data volumes, host networking, privileged containers or host-published Modbus ports. Unique project labels scope resources; `seed` refuses any nonempty volume and creates only a disposable empty MMA2 config and canonical `devices: []` test document.
- MMA2 uses the internal `verify-runtime` bridge. Simulator shares ONLY test MMA2's network namespace because existing Simulator code targets `127.0.0.1` for readiness and Raw Ingest. The Docker host's localhost and real MMA2 are not the namespace used by the test runtime.
- MMA2 runs as root INSIDE its disposable test container only: the seed and Simulator write `/data/config/mma2` as root, while the supervisor must create `restart-ack` in that directory. This is a test-volume permissions accommodation, not a product/Dockerfile/production change. Never use privileged mode.
- OS.js uses a separate `verify-ui` bridge, shared test-only Unix socket in its own `/data` volume, and only `127.0.0.1:18219 -> 18209` for the browser. No Replicator runtime or external Modbus endpoint is started. Use ONLY synthetic `VERIFY-SIM-1`, MMA2 port `15020`, unit ID `1` inside the test namespace.
- Seeding `devices: []` provides an explicit valid empty canonical document. Missing-file Go `devices:null` JSON behavior is NOT tested by this target; do not claim that edge case covered, or seed product/user data outside the test volume.

## Accepted preflight and executable authority

The corrected `37dc61f` JR report recorded sandbox `runtime-ylrnihathakoylzg-68df6b6fb9-6cgp6`, newly created daemon ID `85b789e1-f930-4fc0-968c-935af6ba63f5`, empty Docker containers/volumes, no production resources, free UI port and clean source checkout. The first `e5a1be9` BLOCKED report incorrectly treated an idle daemon as unavailable. The sandbox-local daemon is acceptable because it was freshly initialized inside the disposable OpenHands runtime, not because the default context name proves ownership. Do not reuse a daemon with unknown/production resources.

The COMPLETE, binding live TEST/VERIFY commands, GUI actions, expected result, cleanup and JR report authority are exclusively `## JR TEST TASK — CURRENT: UMIG-004-V LIVE VERIFY` in `handoff.md`. Recheck the sandbox daemon, project ownership and resolved Compose AFTER the test-only seed/permission update before starting anything. An arbitrary second Compose project on the production daemon is still forbidden.

## Cleanup contract

Use the exact project ID and Compose file recorded in the JR packet. After test actions (or a failure), inspect project labels before `docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml down --remove-orphans`; this removes only identified test project containers/networks, never volumes. Record the exact project-labelled `verify-data` volume remaining; do not remove it without separate explicit permission. NEVER use `docker compose down -v`, `docker system prune`, generic container cleanup, wildcards or production-stack commands. Source/build artifacts in the disposable checkout may remain ignored/untracked only if packet authorizes and tracked status stays clean. No test action is permission to modify ICC, product, Docker production files or user data.
