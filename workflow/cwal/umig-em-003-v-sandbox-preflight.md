# UMIG-EM-003-V — ONE-PASS OpenHands sandbox preparation

Purpose: clear the confirmed missing-Docker-daemon blocker and finish the remaining read-only discovery in the existing disposable OpenHands sandbox. This is NOT an executable live concurrency test. No new sandbox or clone required. Human-directed preparation on 2026-09-20 supersedes this file's earlier blanket prohibition on sandbox-local daemon startup and clean fast-forward; all other JR restrictions remain.

## Exact order and stop rules

1. Select the existing disposable OpenHands checkout (prefer `/tmp/em003v-discovery-clone` if it exists). From repository root, read `handoff.md`, `operation cwal.md`, this packet and `workflow/active_work/umig-em-003-v-shared-lock.md`; confirm exactly one ACTIVE task, EM-003-V. Run `git status --porcelain --untracked-files=all`, `git branch --show-current`, `git rev-parse HEAD`, `git rev-parse origin/main`, `git ls-remote --exit-code origin refs/heads/main`. If clean HEAD already equals live main, continue. If stale, the ONLY permitted repository preparation is, exactly ONCE on the clean local `main` branch: `git fetch origin main`, then `git merge-base --is-ancestor HEAD origin/main` (must exit 0), then `git merge --ff-only origin/main`. Repeat the read-only status/HEAD/live-remote checks; require clean and equal. If detached/diverged/dirty or still stale, BLOCKED/STOP; no clone, reset, rebase, force, branch deletion or further fetch. Do not echo Git credential-bearing origin URLs. A normal clean fast-forward is authorized setup, not a product edit.

2. Reuse the prior verified Linux/client evidence if still on this checkout; record `pwd -P`, `uname -s`, `docker context show`, `docker context ls`, `env | grep -E '^(DOCKER_HOST|DOCKER_CONTEXT|COMPOSE_PROJECT_NAME|MCS_VERIFY_OSJS_PORT)=' || true`, `docker version`, and `docker info --format '{{.Name}} {{.DockerRootDir}}'`. Require default local Unix endpoint `/var/run/docker.sock`, no remote Docker host/context and no operator host mounts or credentials; a Docker CLI alone is not daemon proof. If a reachable local daemon exists, DO NOT restart it; proceed to step 3 only when it is the OpenHands sandbox daemon. If server is absent as previously observed (no socket, no running dockerd/containerd), ONE sandbox-only tooling-preparation attempt is authorized: `test ! -S /var/run/docker.sock && ! pgrep -x dockerd && test ! -e /tmp/mcs-em003v-dockerd.pid && sudo -n sh -c 'nohup dockerd > /tmp/mcs-em003v-dockerd.log 2>&1 < /dev/null & echo $! > /tmp/mcs-em003v-dockerd.pid'`. Then `timeout 90s sh -c 'until sudo -n docker version >/dev/null 2>&1; do sleep 2; done'`; record `sudo -n docker version`, `sudo -n docker info --format '{{.Name}} {{.DockerRootDir}}'`, `docker context ls` and the log if startup failed. This one `sudo` exception is ONLY for starting the sandbox-local daemon and reading its metadata/inventory as necessary, never installations, changing daemon config, operator services or other sudo actions. If unavailable or not provably sandbox-local, BLOCKED/STOP once; do not try alternative daemons or hosts. Leave the started sandbox daemon available for the subsequent separately authorized live packet; do not start any application containers here.

3. Read `deploy/verify/compose.yaml`, `deploy/verify/README.md` and `deploy/docker-compose.yml`. Run ONLY `docker compose -f deploy/verify/compose.yaml -p mcs-em003v-discovery config` (no up/start/build/create) and capture exit/stdout/stderr. Require verify-only project, isolated runtime network and namespace, fresh project-scoped volume, only loopback-published UI, no fixed container names, host networking, external/bind/operator mounts, public Modbus/RBE binds. Production Compose is for source comparison only, never execution. Record `ss -ltn` if available; verify loopback UI port is free without binding it.

4. Inventory with read-only `sudo -n docker ps -a --format '{{.ID}} {{.Names}} {{.Labels}} {{.Ports}}'`, `sudo -n docker network ls --format '{{.ID}} {{.Name}} {{.Labels}}'`, `sudo -n docker volume ls --format '{{.Name}} {{.Labels}}'`, and `sudo -n docker compose -f deploy/verify/compose.yaml -p mcs-em003v-discovery ps -a`. Only a NEW unique project will be eligible for later execution; never reuse/delete old volumes or unrelated resources. Unexpected running operator resources, external Docker daemon or port collision => BLOCKED. Do not inspect volume data or secrets.

5. Source-read only: inspect `simulator/runtime_server.go`, `simulator/apply.go`, `replicator/runtime_api.go`, runtime transport/Toolkit adapter, and MMA2 restart acknowledgement paths to report precise local Unix-socket framing, request JSON schemas/fields and restart-observation paths required for ONE bounded test. Do not send requests, invent bodies, write scripts or perform tests. Explicitly identify anything the coding agent must resolve before issuing the live packet.

6. Recheck clean `git status --porcelain --untracked-files=all`, `git rev-parse HEAD` and live remote; disclose one allowed fast-forward and any daemon-start side effects. Report original command output/exits, daemon identity, rendered topology, inventory, source interface findings, and `PREPARATION COMPLETE` or `BLOCKED` IN CHAT; STOP. No Git edit/commit/push; no Docker resources/application start or cleanup because none were created. If preparation fails, state one precise blocker rather than repeating discovery.

## After preparation — NOT AUTHORIZED YET

Coding agent alone writes and reviews the exact live JR packet (fresh project token, synthetic input documents, max two applies per runtime, bounded concurrency, restart/ack and ownership evidence, pre/post hashes, label-scoped `down --remove-orphans` WITHOUT `-v`, retained test volume, report-only transport). No live VERIFY PASS or next-task promotion from this packet.
