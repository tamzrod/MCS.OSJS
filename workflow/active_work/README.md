# Active Work

This directory contains only reviewed, approved, execution-ready microtasks. It is intentionally empty of tasks during the 2026-09-21 Toolkit replanning reset.

Planning and review happen in `workflow/micro_task/`; no task is authorized until placed here. Once placed here, no second approval or promotion is needed to execute its documented scope. Execute one explicitly assigned task at a time, report evidence, then STOP. Do not infer completion of previous tasks or start a successor automatically. Only BLACK SHEEP WALL updates ICC.

The previous OSJT active-work queue and task files were retired from this directory by an explicit human reset, not marked PASS or archived as completed. Their content remains recoverable in Git history. The replacement plan must start from the existing Electron Toolkit as the UI and behavior reference, preserve separate OS.js deployment, and adapt only platform-specific Linux endpoints for YAML load/validate/save, service restart, and applied-state verification. New microtasks must be reviewed before entering this directory.
