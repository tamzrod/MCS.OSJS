// Nameless Classic Icons — icon theme entry (SHELL-004).
//
// Mirrors the @osjs/gnome-icons convention: import the stylesheet that
// emits one `.osjs-icon[data-icon=<name>] { background-image: url(...) }`
// rule per SVG icon, so the OS.js GUI `osjs-icon` element resolves icons
// via the standard CSS background mechanism. No JS registration is needed
// for an icon theme (the desktop applies it by name).
import './main.scss';
