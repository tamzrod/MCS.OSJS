# Handoff

## Human priority — 2026-09-19

The human requested ChatGPT continue CODING while OpenHands is busy on unrelated projects and leave independent TEST/VERIFY to OpenHands later. This explicitly pauses the current Toolkit test queue; it does NOT make any unrun test PASS or satisfy its prerequisites. Existing Docker Compose deployment is reported working by the operator and must not be modified for this preparation task. Pinned one-time Electron renderer donor is `1c971b9a6e00bafadf329df8821421a40cfc079c`. The Toolkit lives in existing OS.js shell; Windows Electron, portable packaging and Modpoll remain separate.

## Authoritative task state and roles

**Sole ACTIVE: UMIG-CF-001 — dormant Memory runtime contract CODE preparation.** User reprioritized from UMIG-003-T on 2026-09-19. UMIG-003 CODE is archived source-only at `fede1715fadd5900da12fd9630793e3514117caf`; its separate UMIG-003-T TEST is QUEUED/NOT RUN and UMIG-003-V VERIFY is QUEUED/NOT RUN. The entire original UMIG chain through UMIG-007A is preserved with all stage gates intact; UMIG-004 still requires UMIG-003-V PASS and is not authorized for execution yet. This new CODE prep has no Next and does not assert a pass or promotion in that chain. DOCKER-001 stays QUEUED/paused; UMIG-008/009 cutover/retirement remain in Planning pending separate human approval.

ChatGPT alone codes, inspects checkpoints and advances workflow after actual evidence. OpenHands/JR uses `operation cwal.md` for TEST/VERIFY only, changes only a currently authorized report and never codes or updates ICC. BLACK SHEEP WALL is the sole ICC editor; the stale ICC cache is not a test prerequisite. Exactly one ACTIVE is the task selector together with this handoff.

## Current CODE boundary

Implement only `workflow/active_work/umig-cf-001-memory-contract-prep.md`: a pure unimported Memory contract module and future JR test file inside the Toolkit/OS.js tree. Inject transport rather than touching a real socket or backend. Preserve version-1 Simulator request/response shapes, unique correlation, honest errors and fail-closed invalid replies. Do not mount it into the fixture renderer, register server side effects, enable Save & Apply, alter Go, Docker, Electron, legacy UI or live configuration. All existing example controls remain disabled, statuses UNKNOWN/UNAVAILABLE and existing Docker volume untouched. Source-only review is not a runnable TEST result.

## Deferred JR packet and evidence

No `JR TEST TASK` is CURRENT while the sole ACTIVE task is CODE. The exact previous UMIG-003-T packet and PENDING report are preserved in immutable `handoff.md` at commit `bc8fe7330969259a8e39a0fa4f533d88078b79cd`. On resumption, republish an updated packet reflecting latest HEAD and sole ACTIVE UMIG-003-T without changing its original renderer checkpoint or skipping UMIG-003-V. Later unit tests for the dormant Memory module require their own exact JR packet; no code-only result is a test PASS. Historical one-window placeholder PASS `7349caa` and initial package discovery PASS `e9d25e3` prove only the earlier scaffold. Four old icon/sound 404s remain observations.

## Next action and recommendation

Next action (ChatGPT CODE): author and inspect the isolated Memory contract and its future test cases, record checkpoint, then stop for explicit continuation/resumption selection. Recommendation: preserve the deployed app, keep the memory adapter dormant, and let OpenHands independently run the ordered original build/GUI tests before any live adapter wiring or write controls.
