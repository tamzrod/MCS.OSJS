# Handoff

## Current authority — 2026-09-20

Human approved the sequence: `UMIG-EM-003-V` live VERIFY first in a NEW disposable OpenHands sandbox, then prepare a separate OpenCode coding trial for `UMIG-EM-004` ONLY after accepted live verification and human promotion. Previous Replicator Go race TEST `UMIG-EM-003-R-T` is archived COMPLETE/PASS; full original evidence at `workflow/archive/umig-em-003-r-t-passed-report.md` and immutable JR commit `04a31475c6427b185c09767703564894bd2791ed`. Unit tests do NOT establish concurrent runtime behavior.

SOLE ACTIVE: `workflow/active_work/umig-em-003-v-shared-lock.md` (VERIFY). EXECUTION BLOCKED pending safety evidence and the subsequent exact executable concurrency packet. `UMIG-EM-004` stays PLANNED; no OpenCode coder source-edit/push authorization. Only BLACK SHEEP WALL updates ICC.

## OPENHANDS PREPARATION — UMIG-EM-003-V — READ-ONLY DISCOVERY ONLY

TARGET: a newly allocated independent disposable OpenHands cloud sandbox, **not** Legion, an operator deployment or the former verification sandbox. The human has selected this target type; actual sandbox daemon isolation is not yet verified. The only authorized action at this stage is the bounded **read-only discovery** described in `workflow/cwal/umig-em-003-v-sandbox-preflight.md`. OpenHands may execute that discovery ONLY in the new sandbox after confirming its source checkout is fresh/clean and no operator deployment is reachable. If uncertain, BLOCKED without Docker operations. Follow each command and STOP on failed freshness, daemon provenance or unexpected resources. Do not invoke the executable `Operation CWAL` lifecycle or claim VERIFY PASS; the live test packet has not been issued.

Discovery output: return command/exit evidence for HEAD versus live remote, sandbox/daemon provenance, rendered `deploy/verify/compose.yaml` topology, existing resource inventory, port occupancy, and actual Simulator/Replicator local apply schemas. A read-only `docker compose config`, `docker ps`, network/volume inventory and `ss` are permissible; `docker compose up/down/build`, daemon startup, config writes, applies, installations, sudo, tests, production Compose and report pushes are NOT. Report `DISCOVERY COMPLETE` or `BLOCKED` IN CHAT and STOP. No repository edits/commit/push or task advancement.

Coding agent then reviews the discovery, pins fresh project/resource token, exact bounded synthetic apply commands and barriers, expected restart/lock observations, hashes and restoration, scoped safe cleanup and one exact executable JR TEST TASK in this handoff. New safety review is required before live Docker execution. Prior `UMIG-EM-003-V` planning document is scope, not execution authority. No current executable live VERIFY packet exists.
