# Handoff — OS.js Toolkit Electron replica

## Autonomous routing authority
Human has approved the ordered OTR roadmap and promotion of its existing microtasks. OPERATION CWAL is the invocation; no additional human prompt, approval or task-selection question is needed for a valid current packet. On every invocation OpenCode reads this handoff, `workflow/active_work/OTR_PROMOTION_QUEUE.md`, and the selected task. Select exactly ONE task per invocation; execute it, report evidence, and STOP. A subsequent invocation selects the next eligible task from persisted evidence. STOP is a per-task boundary, not a request for the human to manually assign each successor. Do not run multiple tasks in one invocation or assume that an external scheduler automatically invokes the next cycle.

## Current task
OTR-001A — OpenCode, read-only DISCOVERY. Packet: `workflow/active_work/otr-001a-electron-shell-inventory.md`. Execute its exact scope now without asking what to do. Source baseline is the observed checkout HEAD. Preserve unrelated dirty files; STOP if Electron source is locally modified. No edits, builds, services, ICC, commit or push. Return the prescribed source inventory in chat and STOP.

## Successor selection on a later invocation
After genuine OTR-001A evidence is available, OTR-001B is the next candidate, then OTR-001C, OTR-002A, OTR-002B, OTR-003A, OTR-003B, in that order. The workflow owner must record completion evidence and the next selected task in this handoff/queue when the task packet grants workflow-write authority; chat-only OTR-001A does NOT grant that authority. If a later invocation cannot establish predecessor evidence, the next packet's exact scope or authorization, report the specific blocker rather than pretending completion. OTR-004..014 parent files are NOT executable: split them into source-pinned atomic child packets after inventories/maps and assign one child at a time. Do not execute a parent as a substitute.

Only BLACK SHEEP WALL edits ICC. No JR TEST task is currently assigned; do not invent JR results. No production YAML writes, live service actions or global restart without explicit task authority.
