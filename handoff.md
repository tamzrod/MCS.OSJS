# Handoff

## Human direction / stage ownership — 2026-09-19

Human approved staged Toolkit migration and a sandbox-local disposable verification environment. ChatGPT owns source/environment preparation, review and task advancement. OpenHands/JR owns ONLY independently instructed TEST/VERIFY through `operation cwal.md`, edits ONLY its permitted report, pushes ONLY `handoff.md`, then stops. Only BLACK SHEEP WALL edits ICC. Preserve operator production Docker, `osjs-data`, user configs, legacy apps and Windows Electron; no production daemon or `down -v`.

## Current selection and predecessor evidence

**SOLE ACTIVE: UMIG-005-V — independent Replicator LIVE VERIFY; NOT RUN / NO PASS.** UMIG-005 CODE archived at `11bea98391b4953ca2356385bf2ffd7164bb657c`. UMIG-005-T UNIT/BUILD PASS report at `4bbe4df852c5ab38c8081abdc15047279b8ceff4` independently reviewed and archived: GitHub diff `f28d5f6..4bbe4df` only changes authorized JR report in `handoff.md`; nine Node tests plus build:local-packages, package:discover and build all exit 0 in disposable Node16, imports/discovery/dual-socket relay inspected, tree clean. This DOES NOT prove live UI or Go runtime. UMIG-005-V successor verified QUEUED with correct Previous, now ACTIVE; UMIG-006 and later tasks QUEUED, UMIG-008/009 cutover/removal Planning, DOCKER-001 paused. Previous Memory live behavioral verification archived with disclosed original Compose restart-command failure; prior retained volume `mcsverify-1789784184-232_verify-data` must not be touched.

Test-only preparation in this checkpoint: `deploy/verify/compose.yaml` now adds private Replicator runtime and seeds `config/replicator/devices.yaml` with `devices: []`; `deploy/verify/README.md` documents the topology and earlier restart defect. These are SOURCE-ONLY edits; Compose has NOT been validated, built or started under this update. All resulting service/config claims require OpenHands' actual observations. No Toolkit implementation, Go, MMA2, production Compose or user data modified.

## JR TEST TASK — CURRENT: UMIG-005-V LIVE VERIFY

GOAL: On the existing disposable OpenHands sandbox ONLY, directly verify actual OS.js Toolkit Replicator canonical load, suggest/inspect (including foreign reservation), one authoritative backend apply, FC3 per-block polling and truthful error/recovery using ONLY sandbox-local synthetic source/destination; restore both test documents and safely remove only project-owned containers/networks. The prior UNIT/BUILD PASS is not evidence for this LIVE VERIFY.

SAFE SETUP / REQUIRED STOP GATES, in order:
1. Fresh disposable checkout of `tamzrod/MCS.OSJS` at latest `origin/main`; record full HEAD and clean `git status --porcelain`; ancestor `git merge-base --is-ancestor 4bbe4df852c5ab38c8081abdc15047279b8ceff4 HEAD` exit 0. If shallow, `git fetch --unshallow origin` allowed. Confirm UMIG-005-V SOLE ACTIVE, UMIG-005-T archived COMPLETE/PASS, UMIG-006 QUEUED. Read `operation cwal.md`, this packet, `deploy/verify/compose.yaml` and README. Never reset/clean tracked files.
2. Verify THIS ephemeral OpenHands sandbox owns its own local Docker daemon. Record hostname, `id`, `docker context show`, `${DOCKER_HOST:-unset}`, `sudo docker info --format '{{.ID}} {{.Name}} {{.DockerRootDir}}'`, Compose version and `sudo docker ps -a`, `sudo docker volume ls`, `sudo docker network ls`. If dockerd idle, prepare locally with `sudo dockerd` and inspect log/socket; no extra VM prerequisite. Check production names and `osjs-data` absent; earlier owned retained Memory volume may exist but MUST be excluded. No remote/production daemon, unrelated containers with uncertain ownership, or use of existing project resources. Check 127.0.0.1 UI port 18219 available using `ss` or `/proc/net/tcp` when ss unavailable (record method); no port assumptions.
3. Set `PROJECT=mcsverify-rep-005-20260919` and `MCS_VERIFY_OSJS_PORT=18219` in shell; confirm `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml config` exit 0 and exactly five services seed/mma2/modbus-simulator-runtime/modbus-replicator-runtime/osjs-shell. Inspect: seed nonempty refusal and all three canonical empty documents; internal MMA2 network; BOTH Go runtimes `network_mode: service:mma2`; UI only `127.0.0.1:18219:18209`; ONLY project-scoped verify-data, no external/bind/production volume, container_name, host/privileged networking, privileged mode, or host Modbus port. `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml ps -a` and Docker ps/volume/network with `--filter "label=com.docker.compose.project=$PROJECT"` must prove ZERO resources for THIS new project. If collision, unknown daemon identity, nonempty target, missing tools after reasonable sandbox preparation or config mismatch: BLOCKED/STOP BEFORE startup. Do not change the project token, reuse Memory data or repair Compose as JR.

EXACT ACTIONS — proceed only if all stop gates pass; record commands, outputs/exits and real browser evidence, STOP PRODUCT TESTING on first contradictory result (FAIL), then perform ONLY verified owned-resource cleanup:
4. Repo ROOT: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml up -d --build`; capture exit, images, `ps -a`, `logs --no-color --tail=120 seed mma2 modbus-simulator-runtime modbus-replicator-runtime osjs-shell`. Expect seed Exited(0), four services Up, no startup error. Poll at most 180 seconds for `curl -fsS http://127.0.0.1:18219/healthz` HTTP 200 shell neutral, and both Unix sockets from the OS.js container: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml exec -T osjs-shell sh -c 'test -S /data/run/modbus-simulator.sock && test -S /data/run/modbus-replicator.sock'` exit 0. Image/setup unavailable after reasonable local prep -> BLOCKED; build/runtime contradictions -> FAIL. No production fallback.
5. From TEST osjs-shell only record `cat /data/config/simulator/devices.yaml /data/config/replicator/devices.yaml /data/config/mma2/config.yaml` and hashes; expect Simulator/Replicator `devices: []`, MMA2 `listeners: []`. Confirm no host Modbus port published via resolved Compose/`docker compose ps`, and only private internal runtime namespace. Launch fresh real Chromium profile at `http://127.0.0.1:18219`; from Start menu click Toolkit once. Require exactly one Toolkit window and Memory/Replicator empty canonical lists (NO Fixture Sim-PLC-1 or Rep-PLC-1), Diagnostics fixture-only; no automatic apply or change to baseline hashes. Capture screenshot, JS errors and WebSocket observations.
6. In Toolkit Memory GUI: Add exactly one synthetic `VERIFY-SIM-1`, port `15020`, unit `1`, FC1–FC4 start `0` count `16`, None/interval 0. Click Memory Save & Apply ONCE. Verify actual canonical Simulator file and MMA2 owners/config, effective private listener `15020/1`, UI success/reload and runtime status; no host 15020 exposure. This is ONLY a source device for the following Replicator test, not a production port. If setup product behavior fails: FAIL/cleanup; no arbitrary direct config injection.
7. Switch to Toolkit Replicator GUI. Observe empty canonical device list, not fixture; capture actual Go-backed `suggest` result (suggestion is advisory, not a reservation). Add exactly `VERIFY-REP-1`; source endpoint `127.0.0.1:15020`, source unit `1`; one Pull Block FC3 start `0` count `16` scan rate `1000`. Turn BOTH destination Auto Port/Auto Unit ID off and set manual destination `15020`/unit `1`; click Check ownership; expect Go result `IN USE`, owner `simulator`, Save & Apply disabled (NO apply attempted on foreign reservation). Set destination to `15021`/unit `1`, Check ownership; expect `AVAILABLE`/no foreign owner and enabled valid Save. If inspection unavailable or conflict not protected, FAIL; no override of ownership.
8. Click Replicator Save & Apply ONCE for only `VERIFY-REP-1`, destination 15021/1. Capture UI, Go logs and actual test-only `/data/config/replicator/devices.yaml`, MMA2 config/owners, timestamps or hashes. Expect one user apply and one Go v1 apply transaction, canonical device persisted after Toolkit close/reopen and destination 15021/1 owned by replicator, while Simulator ownership 15020/1 preserved. Wait up to 90s/one or more scan intervals for direct `status('VERIFY-REP-1')` and UI per-block status: running true, source_status OK only if actual success observed, cycles/last_poll and no fabricated health. All four Network/TCP/Modbus/MMA2 COMMS LEDs must remain UNKNOWN gray even if source poll OK. No host 15021 exposure. If status ERROR unexpectedly, record actual backend error and FAIL rather than labeling healthy.
9. In same Replicator editor change ONLY source endpoint to `127.0.0.1:15999` (unbound localhost in TEST namespace; verify no listener), Save & Apply ONCE; poll up to 90s for actual per-block `ERROR` and backend last_error, with no green COMMS claim. Then restore endpoint `127.0.0.1:15020`, Save & Apply ONCE; expect actual subsequent successful `OK`, advancing cycles/last_poll, canonical endpoint restored, no persistent error. Maximum THREE Replicator apply clicks so far. Do NOT stop/start Compose services (one-shot seed restart defect), probe LAN/Internet or substitute a mock result.
10. Restore disposable documents through GUI: Delete `VERIFY-REP-1` and Save & Apply ONCE; confirm Replicator canonical `devices: []` and 15021/1 reservation released, 15020/1 Simulator ownership preserved. Then Memory Delete `VERIFY-SIM-1` and Save & Apply ONCE; confirm Simulator canonical `devices: []` and no synthetic 15020/1 reservation. These are ONLY test-owned devices. Record final test-only files/hashes/status and pertinent logs. Close browser; record screenshots and JS errors and final tracked git status BEFORE writing report (must be clean). No writes outside test volume.

MANDATORY CLEANUP: after step 4 has created anything, inspect `sudo docker ps -a --filter "label=com.docker.compose.project=$PROJECT"`, `sudo docker volume ls --filter "label=com.docker.compose.project=$PROJECT"`, `sudo docker network ls --filter "label=com.docker.compose.project=$PROJECT"` and verify each targeted resource belongs to THIS exact project. Only then run `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml down --remove-orphans` (NO `-v`); record exit, post-check project containers/networks absent and the exact retained project verify-data volume. If ownership ambiguous do NOT destructively clean; report BLOCKED/remaining assets. Never remove any volume including earlier Memory volume, prune, wildcard cleanup, touch operator containers/config, call production Compose, or bypass the seed guard. No unapproved fixes/retries to force PASS.

EXPECTED/VERDICT: PASS only with independently evidenced fresh project isolation, successful real five-service startup, empty no-fixture baseline, synthetic Simulator source, real Replicator suggestion/foreign OWNERSHIP protection, one authoritative initial apply/persistence and real source/FC3 block polling, truthful loss/recovery, explicit test-only deletion/ownership release and scoped cleanup. Product contradiction=FAIL; indispensable missing evidence/tools after safe local prep=BLOCKED. Distinguish browser vs Go/log/file evidence. Do NOT infer standalone production acceptance, actual 4-layer COMMS probing, missing-file `devices:null`, Windows functionality, Diagnostics, launcher cutover or legacy retirement. A failed required command is not PASS. Record deviations without silently substituting commands.

EVIDENCE: HEAD/ancestor/sole ACTIVE, local daemon/provenance and project resource listings, resolved Compose/build/health/socket results, baseline and post-action canonical YAML/hashes + owners, actual Go apply/inspection/status errors, browser screenshots and JS/WebSocket observations, exactly counted Save & Apply clicks (one Memory seed, up to four Replicator including deletion, one Memory deletion), polling/cycles/last_poll and per-block state, zero host-published Modbus ports, project-only teardown and retained volume label, final clean tracked tree, any unexpected behavior and scope limitations.

REPORT-WRITE AUTHORITY: JR may replace ONLY `## JR TEST REPORT — UMIG-005-V` below, up to `## Next action and recommendation`, with factual PASS/FAIL/BLOCKED, raw command/GUI evidence and any deviations. Commit/push ONLY `handoff.md`, then STOP. No product/Compose/workflow/ICC edits, no fixes, no autonomous successor or unrelated TEST task.

## JR TEST REPORT — UMIG-005-V

PENDING — source-only disposable Replicator service/seed addition and JR LIVE VERIFY packet published. NOT RUN; no Compose/build/browser/Go backend or production verification is claimed.

## Next action and recommendation

Next action: OpenHands execute `OPERATION CWAL` against latest main and this current packet, then STOP. Recommendation: reuse the ephemeral OpenHands sandbox with a new project; retain volumes, distinguish actual FAIL from setup BLOCKED and preserve real evidence before any advancement.
