# Handoff

## Direction and authority — 2026-09-19

Human approved staged Toolkit migration and sandbox-local disposable testing. ChatGPT owns product CODE, exact task-specific packets, JR report review and advancement. OpenHands/JR runs ONLY the current `handoff.md` TEST/VERIFY packet under general `operation cwal.md`; JR edits ONLY its authorized report, commits/pushes ONLY `handoff.md`, then STOPS. Do not modify the general CWAL directive to compensate for task-specific packet errors. Only BLACK SHEEP WALL edits ICC. Never touch operator production Docker, `osjs-data`, user/customer settings, legacy apps or Windows Electron, retained test volumes, or run `down -v`.

## State and immutable evidence

**SOLE ACTIVE: UMIG-006-V — independent live Diagnostics/browser VERIFY; NOT RUN / NO PASS.** UMIG-006 CODE source-only checkpoint `c996794ff14dd65479e84123724f6b9fd9235ee6` archived; UMIG-006-T UNIT/BUILD JR PASS at immutable `handoff.md` commit `0cce2bf662c8f5065182e06c948db73459194479`, independently reviewed. Diff `7b1643490021ff1019bed41eb2126ebdfd0abcb0..0cce2bf` changes ONLY authorized JR report in `handoff.md` (55 additions/1 deletion). JR's 12 focused Diagnostics/Memory/Replicator/fixtures Node tests and 3 build/discovery commands exit 0 in Node16/npm8; fake-DOM disabled actions, read-only observer, independent errors, stale clearing/disposal, source/bundle import isolation, metadata/discovery and tracked-clean verified. That is NOT real browser/backend or service acceptance. Archived TEST: `workflow/archive/umig-006-t-diagnostics-fixtures.md`. UMIG-007+ QUEUED; cutover/removal tasks UMIG-008/009 planning only; DOCKER-001 paused.

UMIG-005-V live core Replicator behavior was accepted with ONE explicitly UNVERIFIED item: actual Toolkit WINDOW close and Start-menu relaunch with canonical Replicator reload/no auto-apply. JR had only switched tabs/reselected. Evidence `handoff.md` at `1a04664e5fdc21e3bd323a97a4f12f3f9d39abdd`, reviewed archive `workflow/archive/umig-005-v-replicator-runtime.md`. THIS packet includes a directly evidenced close/relaunch, not a tab switch or YAML-only inference; no migration/cutover acceptance until observed. Historical retained volumes `mcsverify-1789784184-232_verify-data`, `mcsverify-rep-005-20260919_verify-data` may not be reused/removed. Production and Go/Compose/source unchanged by advancement.

## JR TEST TASK — CURRENT: UMIG-006-V LIVE VERIFY

GOAL: In one NEW disposable real-browser session, verify Diagnostics reads genuine canonical Memory/Replicator device statuses/errors WITHOUT asserting unsupported global service health, service controls or native paths; independently observe genuine Toolkit WINDOW destroy and Start-menu relaunch of a persisted Replicator device with NO automatic apply. Restore only synthetic test configuration and perform scoped cleanup. No product fixes or independent new experiments.

SAFE SETUP / STOP GATES (execute in order; record actual commands/exits):
1. Fresh tracked-clean disposable checkout of `tamzrod/MCS.OSJS` at latest `origin/main`: record full `git rev-parse HEAD`, `origin/main`, `git status --porcelain` (empty). Require `git merge-base --is-ancestor 0cce2bf662c8f5065182e06c948db73459194479 HEAD` exit 0 (if shallow, `git fetch --unshallow origin` read-only permitted). Read `operation cwal.md`, this packet, `deploy/verify/compose.yaml`, README, archived UMIG-006-T and active UMIG-006-V. Verify exactly ONE ACTIVE task UMIG-006-V, predecessor T archived COMPLETE/PASS, UMIG-007 QUEUED. Do not reset/clean/alter tracked files.
2. Prove sandbox-LOCAL Docker daemon: record `hostname; id; docker context show; printenv DOCKER_HOST` (unset acceptable); `sudo docker info --format '{{.ID}} {{.Name}} {{.DockerRootDir}}'`; `sudo docker compose version`; `sudo docker ps -a`; `sudo docker volume ls`; `sudo docker network ls`. If daemon idle, safely start `sudo dockerd` sandbox-local, capture its log/socket, then repeat identity; do not require a separate VM. No remote/operator daemon, production service names or `osjs-data`; previously retained volumes may be present but excluded and never mounted. Unknown unrelated production resource/daemon => BLOCKED. Check 127.0.0.1:18219 listening state via `ss -ltn` or `/proc/net/tcp` and `/proc/net/tcp6` if `ss` absent; must be free. Verify no host Modbus publication.
3. Set `export PROJECT=mcsverify-diag-006-20260919 MCS_VERIFY_OSJS_PORT=18219`. From repo root, run `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml config` (exit 0): exactly seed/mma2/modbus-simulator-runtime/modbus-replicator-runtime/osjs-shell; seed nonempty-volume refusal and three initial empty YAMLs, internal MMA2 bridge, both Go runtimes `network_mode: service:mma2`, isolated verify-data, only OS.js loopback `127.0.0.1:18219:18209`, no external/bind/user volume, host network, fixed container names, privileged mode or host Modbus ports. `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml ps -a` and `sudo docker ps -a`, `sudo docker volume ls`, `sudo docker network ls` each filtered `--filter "label=com.docker.compose.project=$PROJECT"` must show ZERO project resources. Any collision, mismatch, inability to establish ownership/port freedom => BLOCKED/STOP BEFORE startup. Do not select another token to evade a conflict or reuse an earlier volume.

EXACT ACTIONS (once stop gates pass; capture actions, outputs, real Chromium screenshots, browser errors and service evidence; do not run subsequent product steps on genuine FAIL):
4. Repo root: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml up -d --build` ONCE; record exit, `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml ps -a`, `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml logs --tail=120 seed mma2 modbus-simulator-runtime modbus-replicator-runtime osjs-shell` (do NOT use unsupported `docker logs --no-color`). Expect seed Exited(0), four others Up, no startup errors. Poll <=180s for `curl -fsS http://127.0.0.1:18219/healthz` HTTP 200 `status:ok`; prove both sockets from test OS.js: `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml exec -T osjs-shell sh -c 'test -S /data/run/modbus-simulator.sock && test -S /data/run/modbus-replicator.sock'` exit 0. Build/runtime contradiction => FAIL, missing required environment after safe prep => BLOCKED; no production fallback. Never run Compose `up/start` again on populated seed.
5. From test `osjs-shell` capture exact initial contents and SHA256 hashes of `/data/config/simulator/devices.yaml`, `/data/config/replicator/devices.yaml`, `/data/config/mma2/config.yaml` using `cat` and `sha256sum` via `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml exec -T osjs-shell sh -c 'cat /data/config/simulator/devices.yaml /data/config/replicator/devices.yaml /data/config/mma2/config.yaml; sha256sum /data/config/simulator/devices.yaml /data/config/replicator/devices.yaml /data/config/mma2/config.yaml'`: require devices: [] twice, listeners: []. Open NEW real Chromium profile at `http://127.0.0.1:18219`, Start > Development > MCS Modbus Toolkit ONCE. Exactly ONE Toolkit window, Memory/Replicator show empty canonical lists (no Fixture Sim-PLC-1/Rep-PLC-1). Open Diagnostics: each device says `No canonical devices configured`; global MMA2/Simulator/Replicator services `UNKNOWN`; runtime mode and binary/data paths `UNAVAILABLE`; Start/Stop disabled; text pane explicitly states read-only observations, not service logs; no fixture warning. Hashes unchanged after opening/Diagnostics refresh; no auto apply or unexpected JS exception. Screenshot.
6. Create ONLY `VERIFY-DIAG-SIM-1` from Memory GUI: port 15020, unit 1; FC1–FC4 start 0/count 16, simulation None and all intervals 0. Memory Save & Apply ONCE. Verify canonical Simulator YAML, owner 15020/1 simulator, private MMA2 listener, Memory MMA2 RUNNING / Simulation IDLE. No host 15020. Switch Diagnostics or press enabled `Refresh observations` once; within <=30s first canonical Memory device labelled `VERIFY-DIAG-SIM-1 (first of 1 canonical device(s))`, Memory device MMA2 RUNNING, Memory simulation IDLE, Replicator `No canonical devices configured`/UNKNOWN, global services UNKNOWN and native paths UNAVAILABLE. Hashes unchanged by Diagnostics read-only refresh. If contradicted => FAIL/cleanup.
7. Add ONLY `VERIFY-DIAG-REP-1` via Replicator GUI; source `127.0.0.1:15020`, unit 1, ONE FC3 Pull Block start 0/count 16, scan rate 1000; turn Auto Port and Auto Unit ID OFF; set destination 15021/unit 1. Check ownership ONCE: AVAILABLE / no foreign owner, Save enabled. Click Replicator Save & Apply ONCE. Verify canonical `/data/config/replicator/devices.yaml` and MMA2 owners contain 15020/1 simulator PLUS 15021/1 replicator, no host 15021; await <=90s for real Replicator RUNNING, source OK and Block 1 OK with actual last_poll. No additional ownership-conflict retest; UMIG-005-V already covered it. Capture test-only logs, YAML and screenshot.
8. Open Diagnostics tab; press Refresh observations ONCE if needed and wait <=30s. Require `VERIFY-DIAG-SIM-1` / `VERIFY-DIAG-REP-1` labelled first of 1 canonical, Memory MMA2 RUNNING/Simulation IDLE, Replicator device runtime RUNNING/source OK and Pull Block 1 OK, actual last poll detail without fabricated errors. Global MMA2/Simulator/Replicator *service* fields ALL UNKNOWN even when device OK; runtime mode/binary/data paths UNAVAILABLE; Start/Stop visible but DISABLED (inspect rendered `disabled`, do not click); refresh enabled and only read-only. `#log` must say observations NOT runtime service logs. Record screenshot/DOM and browser console/network observations. Record SHA256 of three canonical YAML files AND `/data/config/mma2/owners.yaml`, plus MMA2 restart-request log count/fingerprints, before `Refresh observations`; refresh once and recompare hashes/owners/log count (unchanged). Any Diagnostics apply/config mutation or green fabricated global health => FAIL.
9. MANDATORY carried-forward WINDOW lifecycle check while `VERIFY-DIAG-REP-1` exists: record canonical Replicator YAML and destination 15021/1 and same four hashes/owners/restart fingerprint before close. Using OS.js WINDOW CHROME close **the actual `MCS Modbus Toolkit` window**; browser state/screenshot must show ZERO Toolkit windows (not a tab switch/hide). From desktop Start > Development > MCS Modbus Toolkit click ONCE and show exactly ONE NEW Toolkit window. Enter Replicator: fresh canonical load shows `VERIFY-DIAG-REP-1`, endpoint `127.0.0.1:15020`, destination 15021/unit 1, no fixtures; real status RUNNING/source OK after <=90s. Enter Diagnostics: same canonical device labels and truthful scoped statuses. Re-read four hashes/owners and MMA2 restart request fingerprints/count; UNCHANGED across close/relaunch, no Save & Apply click or auto apply. Screenshot BEFORE close, AFTER zero windows, AFTER relaunch and config hashes. Tab switching, browser page reload or YAML-only inference is NOT PASS; unavailable lifecycle evidence => BLOCKED for reopen subcheck and NO overall UMIG-006-V PASS.
10. Intentionally unavailable synthetic SOURCE (never stop services): in Replicator GUI verify `127.0.0.1:15999` is unbound inside test MMA2 namespace via `/proc/net/tcp` + `tcp6` listening entries (port hex `3E7F`; or `ss` if present). Change ONLY `VERIFY-DIAG-REP-1` source endpoint to `127.0.0.1:15999`, Save & Apply ONCE (Rep apply #2). Confirm canonical YAML changed; wait <=90s for actual backend/UI source ERROR and `connect 127.0.0.1:15999 ... connection refused`. Diagnostics refresh (ONCE if needed, <=30s): Replicator source ERROR, per-block ERROR and real backend `connection refused` in observation pane, independent Memory device still RUNNING/IDLE, global services UNKNOWN, Start/Stop still disabled, paths UNAVAILABLE. The SOURCE is unavailable; do NOT label overall Go service as unavailable/healthy. Screenshot; no direct container stop/start, no localhost beyond approved ports, no new endpoint. Any false healthy/error masking => FAIL.
11. Restore Replicator source endpoint to `127.0.0.1:15020` via GUI; Save & Apply ONCE (Rep apply #3). Wait <=90s for actual successful Block 1 OK/source OK with newer last_poll and no persistent error; Diagnostics refresh shows recovered OK and Memory still IDLE. Then delete ONLY `VERIFY-DIAG-REP-1` in Replicator GUI and Save & Apply ONCE (Rep apply #4): canonical Replicator `devices: []`, 15021/1 reservation released while 15020/1 simulator remains. Delete ONLY `VERIFY-DIAG-SIM-1` in Memory GUI and Save & Apply ONCE (Memory apply #2): canonical Simulator `devices: []`, MMA2 `listeners: []`, owners `reservations: []`; three seeded files' SHA256 exactly match step 5. Diagnostics refresh shows both `No canonical devices configured`, per-device UNKNOWN and global UNKNOWN; record final screenshot/logs/hashes, browser errors and clean `git status --porcelain` BEFORE report. Total MAX six Save & Apply clicks: Memory add/delete 2, Rep add/fault/recovery/delete 4. Close browser. No writes outside this newly labelled test volume.

UI INTERACTION LIMITS: On a rerendered editor obtain a fresh browser state before interacting with the next field. At most TWO bounded corrections per unsaved input/checkbox based on a newly observed state; if it cannot be set, record FAIL instead of source debugging. Never repeat a Save & Apply click because result looks delayed: inspect UI, canonical file and logs and wait within the specified bound. No scripts that directly mutate DOM/model, substitute mock data, edit Go/Toolkit/Compose or patch a runtime to force PASS. Source/log read is allowed only to document an observed contradiction, not to repair it. STOP PRODUCT TESTING on first real contradiction; preserve evidence. If failing mid-sequence, do not invent unscripted GUI restoration; leave project data retained and perform owned-resource teardown only if safe.

MANDATORY CLEANUP even after FAIL/BLOCKED once resources exist: enumerate `sudo docker ps -a --filter "label=com.docker.compose.project=$PROJECT"`, `sudo docker volume ls --filter "label=com.docker.compose.project=$PROJECT"`, `sudo docker network ls --filter "label=com.docker.compose.project=$PROJECT"`; require each targeted container/network/volume label EXACT project and no unrelated resources in deletion set. ONLY then run `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml down --remove-orphans` WITHOUT `-v`; record exit, project containers/networks absent, exact retained volume and label. If ownership ambiguous, do not remove anything, report BLOCKED and inventory. No prune, wildcard cleanup, force rm or removal of any volume, no production Compose/daemon or known previous project volume. Do not restart seeded stack after initial up. If failure occurs before resource creation, no destructive cleanup.

VERDICT / EVIDENCE: PASS only if all required stop gates, real healthy Diagnostics, truthful synthetic source ERROR and recovery, explicitly DISABLED native controls/global UNKNOWN, no Diagnostics writes, actual WINDOW close/Start-menu relaunch plus unchanged hash/owners/no-auto-apply, original empty restoration and scoped teardown directly observed. Genuine product contradiction => FAIL; essential missing environment or missing WINDOW evidence after safe permitted prep => BLOCKED. Capture full HEAD/ancestor, daemon provenance, resolved Compose/project label/health/sockets, exact commands/output/exits, real Chromium screenshots/JS errors, UI interactions and counts, canonical YAML+SHA256/owners and backend logs at each boundary, scoped cleanup, retained volume and final clean tree, deviations and limits. Do not infer Windows controls, global Docker/MMA2 health, COMMS probes, real service logs, production deployment, launcher cutover, 3-tab parity or missing-file null handling.

REPORT-WRITE AUTHORITY: JR may replace ONLY `## JR TEST REPORT — UMIG-006-V` below up to `## Next action and recommendation` with factual PASS/FAIL/BLOCKED and evidence. Commit/push ONLY `handoff.md`, then STOP. No CODE, Compose, workflow, archive, ICC, `operation cwal.md` or other handoff section edits; no autonomous successor.

## JR TEST REPORT — UMIG-006-V

PENDING — live Diagnostics and real Toolkit window close/relaunch NOT RUN / NO PASS. Source/packet only; no sandbox Docker, browser, Go or config execution by ChatGPT.

## Next action and recommendation

Next action: Run `OPERATION CWAL` in OpenHands at latest main against THIS UMIG-006-V packet, then STOP. Recommendation: reuse the sandbox with a NEW uniquely owned project, preserve retained volumes, report the exact first product contradiction without debugging it, and distinguish the mandatory real WINDOW check from tab switching.
