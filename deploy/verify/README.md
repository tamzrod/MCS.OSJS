# Disposable Toolkit verification stack — Memory and Replicator

Use ONLY `deploy/verify/compose.yaml` on the disposable OpenHands sandbox-local Docker daemon with a fresh unique project. Never combine this with `deploy/docker-compose.yml`, connect to the operator daemon, use `osjs-data`, or run `down -v`. The earlier Memory live test is archived at `d230f60`/`workflow/archive/umig-004-v-memory-runtime.md`; its retained volume `mcsverify-1789784184-232_verify-data` must NOT be reused or removed. If sandbox dockerd is idle, safe `sudo dockerd` startup is test tooling preparation, not an automatic BLOCKED condition. Recheck local daemon provenance, labels, production-resource absence and port before startup.

## Scope and topology

Five services: one-shot `seed`, `mma2`, `modbus-simulator-runtime`, `modbus-replicator-runtime`, `osjs-shell`. A NEW `verify-data` volume is required: seed refuses any nonempty volume, then writes test-only `listeners: []`, Simulator `devices: []`, and Replicator `devices: []`. Do not bypass, weaken or re-run the seed on a populated volume. Only a new project with verified absent resources may start. Both Go runtimes share ONLY test MMA2's private internal network namespace; Simulator `127.0.0.1:15020` source and Replicator destination `15021` are never host published or external. OS.js uses a separate UI bridge and exposes ONLY loopback browser port `127.0.0.1:18219`. All services share ONLY the fresh test volume for their Unix sockets/config. Test MMA2 runs as root inside its own unprivileged container to write restart acknowledgments. No fixed container names, host network, host-published Modbus ports, production mounts or external volumes. Do not point the Replicator source to any customer, host, LAN or Internet address; use exclusively private 127.0.0.1:15020 for success and 127.0.0.1:15999 to simulate a failed local source.

## One-shot seed and prior packet defect

In the earlier Memory test, the packet's `docker compose start modbus-simulator-runtime` FAILED exit 1 because Compose re-evaluated the one-shot seed against a populated volume. That historical command did NOT pass. If a future packet explicitly requires restarting one service, first resolve the exact stopped container ID and verify its project and service Docker labels; only then `sudo docker start <exact-verified-ID>`. Never guess IDs, rerun `compose up/start` against a populated seed, change the guard, or touch another project. UMIG-005-V uses a synthetic configuration change to observe source error/recovery and requires NO direct container restart. The archived packet is not current authority.

## Live test and cleanup authority

ONLY the current sole ACTIVE `UMIG-005-V` packet in `handoff.md` authorizes exact JR commands, browser actions, expectations and report. The disposable source is configured through Toolkit Memory first; Replicator manual ownership is checked against that Simulator-owned port before a separate test-only destination is applied. Store document, owners, UI and backend status proof, then delete Replicator and Simulator test devices through their own GUI Save & Apply to restore the seeded empty documents. On contradictions, stop product testing and record FAIL; clean up only verified owned test resources. Before teardown verify container, volume and network project labels, then `sudo docker compose --project-name "$PROJECT" -f deploy/verify/compose.yaml down --remove-orphans` WITHOUT `-v`. Record the retained test-only volume; never remove even it without separate authorization. No generic prune, wildcard cleanup, production commands, ICC edits or UI cutover. Missing-device-file JSON null behavior, production Docker health, Windows COMMS and Diagnostics are not verified by this stack.
