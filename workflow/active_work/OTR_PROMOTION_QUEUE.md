# OTR promotion queue
Human authorization: promote all OTR microtasks for the Electron Toolkit -> OS.js replica. Exactly one task is ACTIVE at a time: OTR-001A. All other OTR tasks are QUEUED; placement is approval, not permission to run an unassigned task. OpenCode must execute one assigned task, report evidence, STOP. Existing parent OTR-001..014 are phase containers where split children exist. OTR-004..014 are approved queue scope only and must be converted to source-pinned, atomic CODE/TEST/VERIFY packets after OTR-001..003 inventories; no guessed source paths, backend contracts, commands, or production writes. No ICC edits; BLACK SHEEP WALL only. Preserve dirty local changes; stop on overlapping edits. No automatic successor execution, no false PASS. See planning/microtask/otr-sizing-and-decomposition-review.md.