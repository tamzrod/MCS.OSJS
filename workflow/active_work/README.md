# Active work — approved work only

`workflow/active_work/` contains canonical approved, unfinished task packets. Presence here means eligible for consideration, not permission to bypass the packet's `Blocker Task`, assigned agent, missing input or safety gates. `planning/microtask/` contains unpromoted planning copies and is not executable. `workflow/archive/` contains only completed canonical packets with verified acceptance evidence and delivery. An evidence report alone does not prove completion.

`FMT_PROMOTION_QUEUE.md` is the approved FMT task manifest, not a status ledger. Root `handoff.md` points to the approved queue, not an individual ACTIVE packet. At each CWAL invocation, enumerate canonical active packets, verify dependencies by archived canonical packets and actual evidence, select one executable task for the current agent, complete or document its obstacle, then rescan independent work. No task-level STOP ends the queue coordinator loop. Keep blocked work in active_work and document the precise reason in the run report; never archive a blocked, failed, unverified or undelivered task.

FMT-005 needs separately approved exact build command and environment and must run only under its assigned independent TEST agent. FMT-008 belongs to ChatGPT and must not be impersonated by OpenCode. Neither is authorized for execution by workflow migration. The OTR queue and historical OTR packets are retired and non-executable even if legacy files remain here pending evidence-backed reconciliation. Do not delete historical evidence or treat disputed archives as truth. Only BLACK SHEEP WALL edits ICC; no unauthorized product changes, production access, destructive cleanup, merge, force push or main push.
