# Handoff

## Current authority — 2026-09-20

`UMIG-EM-002-V` is archived COMPLETE / live VERIFY PASS after ChatGPT reviewed OpenHands' independent packet/report at `3085be499ba43e0d3cf7cef70a8a30c837fe0f46`. That historical commit preserves the full original packet and report. Its bounded proof covered one disposable synthetic Memory -> Replicator FC3 -> MMA2/Diagnostics workflow with true timestamps, four GUI applies, restored seed hashes and label-verified teardown. It does NOT establish multiwriter lock, advanced UI, true full COMMS, network output permission, deployment or production readiness. Earlier UMIG-EM-002-B and R are archived TEST PASS in their separate scopes.

Human explicitly approved `UMIG-EM-003` CODE splitting followed by a single bounded OpenCode V2 JR unit-TEST trial. Original CODE sizing 6 was split at genuine transaction boundaries: `UMIG-EM-003-L` lock primitive, `UMIG-EM-003-S` Simulator wiring, `UMIG-EM-003` Replicator wiring and final shared-path audit, then `UMIG-EM-003-T` independent Go TEST. `UMIG-EM-003-V` remains PLANNED and needs separate human approval. No automatic wholesale JR replacement.

`UMIG-EM-003-L` is archived COMPLETE / SOURCE ONLY. Source checkpoint: `mma2composer/writer_lock.go` at `a61dabc60226d97c822fc55673146ce7543279f5`, Linux `writer_lock_linux.go` at `5717ff0c8451a888a441d7af789ed91616d7e1c6`, non-Linux `writer_lock_other.go` at `ada0dda2b4ffe2434ec06311f4319a58a22b37f8`, focused unrun test source `writer_lock_linux_test.go` at `6dfb746e152d92c856e495691b0eac2bee182245`. All four files were read back from GitHub and `c9baf86..main` changed-path inventory was reviewed at the checkpoint. This is NOT tested or integrated multiwriter protection; no build, go test, Docker or operator action was performed. Lock uses a shared Linux private advisory `flock` file at `config/mma2/.writer.lock` with bounded wait and a single outer transaction; Windows behavior is unchanged by the platform fallback.

The SOLE ACTIVE task is `workflow/active_work/umig-em-003-s-simulator-lock.md` (CODE / ChatGPT). `workflow/active_work/umig-em-003-shared-lock.md` is QUEUED CODE and `workflow/active_work/umig-em-003-t-shared-lock.md` is QUEUED TEST. Only after BOTH remaining CODE stages have source checkpoints reviewed and archived may ChatGPT activate TEST with one fresh exact packet and source SHA. No source build/test PASS is claimed yet. ICC remains stale; only BLACK SHEEP WALL may edit ICC.

## JR TEST TASK — NONE / NOT AUTHORIZED

Do not invoke Operation CWAL now: ACTIVE is CODE and no test packet exists. The completed UMIG-EM-002-V packet is closed and must not be repeated. Experimental OpenCode in `~/apps/MCS.OSJS-jr` on the operator Legion is authorized as a ONE-TASK trial ONLY when `UMIG-EM-003-T` becomes ACTIVE with an exact new handoff. Its `~/opencode-smoke/opencode.jsonc` system text still forbids CWAL; amend that local line only at the future test gate, not now. Retain shell approval/edit denial, never use `--auto`. Worktree is NOT a security sandbox. No Legion Docker, sudo, Modbus endpoint, operator data or live verification by default. OpenHands remains the existing official fallback. JR never modifies product/workflow/ICC or advances task state.

## Next CODE action

Implement the Simulator outermost non-nested Linux writer-lock boundary across actual applicable writers and restart acknowledgment under `UMIG-EM-003-S`; audit source entry points and preserve error truth. Record source readback and checkpoint only, archive S and advance the explicit QUEUED UMIG-EM-003 when its own CODE gate is met. After completion of the Replicator/final-audit CODE, activate the queued TEST and author one exact local-only unit test Operation CWAL packet for OpenCode. Do not infer PASS, touch production, enable RBE, or edit ICC.
