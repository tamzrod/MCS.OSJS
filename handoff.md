# Handoff

## Current authority — 2026-09-20

`UMIG-EM-002-V` is COMPLETE / live VERIFY PASS. ChatGPT reviewed the independently executed OpenHands Operation CWAL report at commit `3085be499ba43e0d3cf7cef70a8a30c837fe0f46` against the original exact packet; the original packet and full factual report remain permanently available in that commit's `handoff.md`. Its only report-transport change was `handoff.md`. The verified scope was ONE new empty isolated Compose stack, health and two Unix sockets, real Chromium synthetic Memory -> Replicator FC3 -> MMA2/Diagnostics operation, truthful global UNKNOWN status, four total GUI Save & Apply clicks, exact restoration of three seed file SHA256s, and label-verified `down --remove-orphans` WITHOUT `-v`, retaining only the newly owned test volume. No source or Compose edits and no production access were reported. See `workflow/archive/umig-em-002-v-upgraded-baseline.md` for the adjudicated limits and provenance.

Predecessors `UMIG-EM-002-R` and `UMIG-EM-002-B` are archived COMPLETE / PASS within their own bounded TEST scopes. Product source checkpoint remains `c98eacef8a6af8b0786a09766a4a034e25e82ce6`; intervening commits changed only workflow and handoff files. This is NOT a PASS for advanced OS.js UI, cross-process lock, full COMMS, privileged MMA edits, network output exposure, installed Electron, visual parity or production.

There is now ZERO ACTIVE task: the completed `UMIG-EM-002-V` was archived. This is an intentional STOP at the human promotion boundary, not permission to infer a successor. `planning/microtask/umig-em-003-shared-lock.md` is the next named task but is still PLANNED CODE / ChatGPT, sizing 6; it requires explicit human promotion and task split/review before any implementation. Its TEST and VERIFY stages are also PLANNED. ICC remains stale; only BLACK SHEEP WALL may edit it.

## JR TEST TASK — NONE / NOT AUTHORIZED

The previous `UMIG-EM-002-V` packet is CLOSED and must not be rerun. Do not invoke Operation CWAL on this handoff: absent a sole ACTIVE TEST/VERIFY task and a fresh exact packet, JR must report BLOCKED and STOP. In particular, OpenCode on the operator's Legion has not executed or earned any JR PASS for MCS.OSJS; the experimental profile/worktree does not itself grant authority to run tests or access operator Docker, persistent settings or services. Any future OpenCode trial must use a new human-authorized TEST task, a coding-agent-committed source checkpoint, exact safe commands and a reviewed local-only environment; no automatic replacement of OpenHands or reuse of the closed report.

## Next action and recommendation

Human: approve or decline promotion/splitting of `UMIG-EM-003` shared Go writer-lock CODE. If approved, ChatGPT first scopes/splits and implements the smallest authorized CODE checkpoint, then separately promotes its TEST stage with an exact Operation CWAL packet. At that gate, human may authorize OpenCode JR for a bounded local, non-production unit test in its separate worktree, with shell approval required and edits denied. Do not run live Docker verification on the Legion by default.
