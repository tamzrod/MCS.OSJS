# Handoff

## Human direction — CURRENT Electron MCS Toolkit is the model (2026-09-19)

The human changed the migration course from visual comparison with an OLD snapshot to model/function alignment with the CURRENT Electron MCS Modbus Toolkit. Latest assessed reference `1ca174058548996913bdcc13c7e2ef224a768457` includes Electron Memory advances in `ff6846f973ad973bd3270b0ff2d707ce5c2616c9` and newly pushed Go Replicator COMMS observations in `1ca1740`. Frozen one-time visual donor `1c971b9a6e00bafadf329df8821421a40cfc079c` remains immutable history only. Evaluate and explicitly re-pin any later Electron/Go push; never auto-sync or import Electron renderer, IPC, preload, Windows service controls, paths or filesystem implementations. Go/MMA2 owns config validation, composer/ownership and apply; Docker OS.js uses explicit authenticated Toolkit contracts and window-scoped UI. Platform-only features remain disabled/UNAVAILABLE, missing statuses UNKNOWN, never fabricated green.

ChatGPT owns CODE, task-specific JR packet, report adjudication and workflow advancement. OpenHands/JR is TEST/VERIFY ONLY under general `operation cwal.md`, edits only its authorized report and stops. Only BLACK SHEEP WALL edits ICC. Do not modify ICC or general CWAL via migration replanning, or touch production Docker, operator data/`osjs-data`, customer devices, Electron installation, legacy UI or retained test volumes.

## Preserved evidence and changed-source caveat

UMIG-006-V live Diagnostics and actual Toolkit window close/relaunch PASS was independently reviewed/archived at `ec390c0540d5aa07d7f5ad7f404c62690fd090c3`; JR report `f6b7549b2c31e4fcf22427d8dee00b181f4b7186`. That PASS proves its exact tested SHA only; NOT newly merged MMA2/RBE, new Replicator COMMS, advanced Memory UI, shared settings, installed runtime, visual parity, production or cutover. `ec390c0..ff6846f` introduced substantial MMA2/Simulator/composer/advanced Electron changes; `ff6846f..1ca1740` introduced measured Replicator network/TCP/Modbus/MMA2 per-cycle/per-block status in Go and Electron. These changes were source-reviewed for planning, NOT rebuilt/run by ChatGPT; commit-reported tests do not replace JR's independent gates. Retained volumes `mcsverify-1789784184-232_verify-data` (if present), `mcsverify-rep-005-20260919_verify-data`, `mcsverify-diag-006-20260919_verify-data`: never reuse/delete without separate authority.

## Gap and revised order

Electron Memory has Device Definition/Advanced folder tabs, per-device RBE Rules/State Sealing/Access Policy, separate shared MMA Settings modal and independent drafts. Electron `mma-load`/`mma-apply` uses host IPC and bundled MMA2 validation; no equivalent operation exists in Go Simulator v1 Unix protocol or Toolkit `memory-contract.js`/`server.js` (only `load/apply/status`). OS.js Memory editor has only basic FC/None-Random form. Go Simulator/composer now models advanced per-device config and preserves foreign ownership, but current UI and live compatibility are unverified. New Go Replicator now exposes real COMMS layer telemetry; current OS.js four LEDs intentionally remain UNKNOWN and must not turn green without fresh correlated observation checks. RBE TCP draft is NOT LOCKED and supplies neither subscriber authentication nor replay/production latency proof; never enable/expose it by shortcut.

New sequence: (1) approve source/field/API/telemetry ownership contract; (2) separately implement and independently TEST/VERIFY upgraded Go/MMA2 baseline and authenticated shared-settings management with full config validation, foreign ownership preservation and safe failure; (3) per-device advanced editing/drafts/round-trip with independent test and live verify; (4) shared modal and scoped global apply with independent test/live verify; (5) per-block backend-derived Replicator COMMS mapping with fresh/wrong-device/stale/legacy fallbacks and independent tests/live verify; (6) UMIG-007 current-model visual parity after updated approved donor capture; (7) UMIG-007A independent deployment. UMIG-008/009 launcher cutover/legacy removal remain separate human approval, not automatic. Exact microtasks may be sequenced/refined only through approved contract stage.

## Workflow stop — ZERO ACTIVE / NO CURRENT JR PACKET

UMIG-006-V archived COMPLETE/PASS. UMIG-007 is QUEUED/DEFERRED; old visual-only packet would verify the WRONG model, and current-model donor images are not pinned. New `planning/microtask/umig-em-001-electron-model-contract.md` is PLANNED ONLY, not promoted. UMIG-007A QUEUED, cutover tasks planning/approval only. No ACTIVE task or JR TEST TASK exists. Do not invoke `OPERATION CWAL`, run Docker or change source based on planning alone. Human promotion of the bounded contract task and subsequent independently sized CODE/TEST/VERIFY stages is required.

## Next action and recommendation

Next action: human review/promote UMIG-EM-001 compatibility/ownership contract; ChatGPT then author small successor microtasks. Recommendation: preserve current working versions and safety boundaries; no repeated old Diagnostics/visual-parity tests or unsupported COMMS/production claims.
