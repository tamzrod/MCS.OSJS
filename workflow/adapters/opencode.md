# OpenCode adapter — Ubuntu workstation / Qwen

Work only on `opencode`; record branch, HEAD and `git status --short`. Preserve unrelated changes. Never switch, reset, clean, restore, stash, rebase, merge, force-push, touch another worktree, edit ICC or push main. Fetch/pull requires separate authorization.

Read `AGENTS.md`, `operation cwal.md`, `planning/microtask/rules.md`, root `handoff.md` and its approved queue. `workflow/handoff.md` is a pointer only. The queue is an approved ID/constraint manifest, not a status database. `workflow/active_work/` contains unfinished approved canonical packets; `workflow/archive/` contains completed canonical packets only when verified evidence and delivery exist. Planning duplicates and legacy `Status:` fields are not authoritative. Do not checkpoint ACTIVE in three files; derive current work from active_work, task evidence and this invocation's run log.

For every approved task, check dependencies by verified archive evidence, assigned agent and exact scope. Select the lowest-ID eligible OpenCode packet, execute immediately without asking to activate or select. Complete evidence and authorized delivery, archive only on verified completion, then rescan. A packet STOP/RETURN ends that task only. If blocked, failed, missing authority, inconsistent or another agent's work, record its exact reason, leave it in active_work, skip it for this invocation and continue independent eligible OpenCode tasks. Never fabricate evidence, self-certify JR verification or execute unauthorized FMT-005/FMT-008. No repeat attempts in one invocation without new authorization.

At end report each approved ID exactly once, archived evidence or active_work obstacle and next actor. Stop only when no eligible OpenCode work remains or a genuine global safety/integrity issue prevents reliable selection. Successful commit/push, compaction and progress summaries do not by themselves terminate approved queue execution. Do not ask `Should I continue?` while executable work remains.
