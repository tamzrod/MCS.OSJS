# UMIG-EM-003-R-T — TEST: Pre/post-commit Replicator recovery regression

Status: ACTIVE — independent OpenCode V2 JR on separate Legion worktree. Human approved focused correction followed by new exact test packet on 2026-09-20.
Stage: TEST. Previous: `UMIG-EM-003-R` archived SOURCE ONLY at `538324a472eb15ff8ef97ee66f826fa02ba9462f`. Next: NONE; `UMIG-EM-003-V` remains PLANNED, needs independent human promotion and safely isolated runtime target. OpenHands standby only.

Acceptance: run ONLY the exact currently activated `handoff.md` JR packet, source-pinned to checkpoint `538324a...` and current live GitHub `main`; one clean detached worktree, single ACTIVE, exactly allowed workflow-only source-to-activation diff, Go >=1.25, E2E disabled and offline cache. Execute the one `replicator` `go test -race -count=1 -timeout=300s -v ./...` suite once via named approved fixed runner. Real `--- PASS` required for `TestApplyPreCommitFailureRestoresPreviousPollers`, `TestApplyPostCommitRestartFailureStopsPreviousPollers`, `TestManagerCommittedUnacknowledgedRestartFailsClosed`, `TestRuntimeManagerApplyLifecycleAndStatus` plus package OK and no other product failures. Full original stdout/stderr, exits, clean unchanged post-state, handoff-only report and verified non-force push required. If product fails report FAIL, if unsafe/missing evidence BLOCKED, if run/report transport interrupted disclose INCOMPLETE, never invent PASS. Runner's scoped handoff-only report write/push expressly approved; no other edit, product fix, staging, task changes, installations, production, Docker, host devices, non-test network endpoints, or automatic shell approval. Post-push JR STOP. Go TEST does not establish live VERIFY.
